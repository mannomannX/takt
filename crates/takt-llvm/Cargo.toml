[package]
name = "takt-llvm"
version = "0.1.0"
description = "Takt: Codegen nach LLVM-IR (Referenz 11.2), als Text"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
takt-diag = { path = "../takt-diag" }
takt-mir = { path = "../takt-mir" }

[lints]
workspace = true

[dev-dependencies]
# Nur fuer die Tests: Sie uebersetzen echte Korpusprogramme, statt MIR zu
# erfinden — ein Struct, der nur fuer Testdaten stimmt, ist kein Beleg.
takt-sema = { path = "../takt-sema" }
takt-diag = { path = "../takt-diag" }
# Das Orakel des Mustervergleichs: Der erzeugte Code muss mit ihm
# uebereinstimmen (8.7, Satz 9.4.4).
takt-match = { path = "../takt-match" }
