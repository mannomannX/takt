# Das Ziel des Bring-up-Programms.
#
# Die Linker-Argumente stehen *nicht* hier, sondern in `build.rs`: Diese
# Datei gilt nur, wenn `cargo` aus diesem Verzeichnis laeuft, und ein
# Bau von aussen erzeugte sonst still eine Binaerdatei ohne
# Vektortabelle.

[build]
target = "thumbv7em-none-eabihf"
