system:
    language = 1
    tick     = 1 ms

# `resume` an einem zusammengesetzten Zustand mit `initial` (5.12).

output n : int in 0..99 @ hw("o/n") with safe = 0

machine m:
    initial RUN

    state RUN resume:
        initial SUB

        state SUB:
            loop:
                n = 1
