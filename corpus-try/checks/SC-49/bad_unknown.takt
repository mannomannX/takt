# SC-49: unbekannte Edition.

system:
    language = 9  # ~ SC-49
    tick     = 10 ms

output led : bool @ hw("gpio/led") with safe = false

machine m:
    initial RUN

    state RUN:
        loop:
            led = true
