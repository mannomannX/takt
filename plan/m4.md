# M4 — Codegen Linux

Stand 2026-09-11. Bezug: `plan/plan.md` Abschnitt 4 (Meilensteintabelle),
`plan/definition.md` v0.2.8 (4.2, 11.1, 12.1–12.6, 13.8), `plan/mir.md`,
`plan/features.csv` (50 M4-Einträge), `plan/feedback.csv`.

M3 ist abgeschlossen: Die Intervallanalyse steuert die
Darstellungsverengung, das Kostenmodell steht, `takt size` rechnet. Damit
ist die Bedingung erfüllt, die der Plan für M4 nennt — *„ohne
Intervallanalyse bekäme jede Stelle einen Laufzeit-Check, der Codegen
würde später umgebaut."*

---

## 0. Umfang und Abnahme

Aus der Meilensteintabelle:

> `takt-llvm` für x86-64/aarch64, strikte FP, IEEE-Modus, `fma`;
> `libtaktm` beider Breiten mit Konformitätsvektoren; `takt-rt-core` +
> `linux_rt` (PREEMPT_RT, Doppelpuffer, Treiber-Threads,
> Rand-Selbstprüfungen); Simulationstreiber als HAL-Implementierung;
> Record/Replay; `takt run`, `takt replay`
>
> **Exit:** differentielles Testen: Interpreter ≡ nativ (x86-64 ≡
> aarch64) auf Korpus und Fuzzer; 14.1–14.6 in Echtzeit auf der Box;
> Replay reproduziert Läufe bitgenau

Fünf Crates entstehen neu: `takt-llvm`, `takt-rt-core`, `takt-rt-linux`,
`takt-hal`, `libtaktm`, dazu `takt-conformance`. Heute gibt es sechs
Crates mit rund 39 000 Zeilen; M4 ist der größte Zuwachs seit M0.

---

## 1. Der Befund, der die Reihenfolge bestimmt

**Satz 9.4.4 ist heute unbewiesen — und zwar nicht erst im Codegen,
sondern schon im Interpreter.**

`crates/takt-interp/src/call.rs:192` rechnet die transzendenten Funktionen
mit Rusts Standardbibliothek:

```rust
Intrinsic::Sin => v.sin(),
Intrinsic::Cos => v.cos(),
Intrinsic::Exp => v.exp(),
```

`f64::sin` delegiert an die Plattform-libm. Deren Ergebnis ist **nicht
über Targets garantiert**: glibc, musl, MSVC und die ARM-Bibliotheken
unterscheiden sich im letzten Bit, und selbst dieselbe glibc liefert auf
x86-64 und aarch64 für manche Argumente verschiedene Werte. 4.2 verlangt
dagegen ausdrücklich „korrekt gerundete Implementierungen (Ansatz
CORE-MATH) … auf allen Targets identisch kompiliert".

Betroffen sind 11 der 26 Intrinsics: `sqrt`, `sin`, `cos`, `tan`, `asin`,
`acos`, `atan`, `atan2`, `exp`, `log`, `pow`. (`sqrt` ist ein
IEEE-Hardwarebefehl und damit sicher; `fma` nutzt `mul_add`, das die
IEEE-754-Zusage trägt — beide sind nur der Vollständigkeit halber
genannt.) Die übrigen 15 sind Integer- oder Vergleichsoperationen und
target-unabhängig.

**Das ist der Grund, `libtaktm` zuerst zu bauen, nicht als Beiwerk.**
Solange der Interpreter die Plattform-libm benutzt, misst das
differentielle Testen den Codegen gegen ein Orakel, das selbst nicht
reproduzierbar ist. Ein Unterschied zwischen x86-64 und aarch64 wäre dann
nicht zuzuordnen: Fehler im Codegen oder Unterschied in der libm?

**Die gute Nachricht: Die Naht ist klein und war vorgesehen.** `plan/m1.md`
Abschnitt 8 sagt es wörtlich — die Mathematik komme aus
„`libtaktm`-Vorläufern (Rust-`f64`/`f32`-Funktionen, in M4 durch korrekt
gerundete ersetzt; der Interpreter kapselt sie an einer Stelle)". Die
Kapselung ist zwei Closures (`call.rs:191` `apply32`, `call.rs:201`
`apply64`) plus vier Arme für `sqrt`, `atan2`, `pow` und `fma`. Der
Austausch ist ein überschaubarer Eingriff an einer Stelle, kein Umbau.

**Drei weitere Befunde aus der Bestandsaufnahme**, die den Zuschnitt
ändern:

1. **`f32` ist vollständig implementiert**, kein Stummel — eigene
   `Value`-Variante, native `f32`-Arithmetik ohne Doppelrundung
   (`arith.rs:127`), Tests bis zur Bitgleichheit. Beide Breiten brauchen
   ihre Vektoren also ab dem ersten Tag, nicht nacheinander.
2. **`finite()` (`arith.rs:103`) ist das einzige Tor für nicht-endliche
   Werte.** Kein NaN und kein Unendlich erreicht je einen `Value`; jeder
   Versuch ist ein `ArithmeticFault(NonFinite)`. Der Codegen muss diese
   Semantik nachbilden — sie ist kein Nebenschauplatz, sondern die
   Totalitätszusage aus 4.1. Dazu kommen die Domänenwächter für `asin`,
   `acos`, `log` (`call.rs:185–189`) und die NaN-Prüfungen bei `atan2`
   und `pow` (`call.rs:221`, `228`).
3. **Native Funktionen sind reines Gerüst.** `call_native`
   (`call.rs:126`) trapt mit einer Meldung, die M4 selbst nennt, und das
   Prelude enthält **keine einzige** `native fn` — obwohl `plan/m1.md`
   `crc32`, `sha256_*` und `fft256` dort ansagt. Struktur, Kostenvertrag
   und Senkung stehen; die Implementierung fehlt ganz.

---

## 2. Abwägungen

### 2.1 Wann kommt `libtaktm`?

| | zuletzt (nach dem Codegen) | parallel zum Codegen | **zuerst, vor dem Codegen** |
|---|---|---|---|
| differentielles Testen aussagekräftig | nein: zwei Unbekannte | teilweise | ja: eine Unbekannte |
| Interpreter bleibt gültiges Orakel | nein | wird es unterwegs | ja, ab Schritt 1 |
| blockiert den Codegen | — | nein | nur die transzendenten Fälle |
| Risiko bei Fehlschlag sichtbar | spät | mittel | **früh** |

**Entscheidung: `libtaktm` ist Schritt 1, vor dem ersten LLVM-Baustein.**

Begründung: Der Plan nennt „Bit-Identität scheitert an FP-Details" als
größtes M4-Risiko und als Gegenmaßnahme „`libtaktm`-Vektoren". Eine
Gegenmaßnahme, die erst nach dem Risiko kommt, ist keine. Vor allem aber
ist `libtaktm` **unabhängig vom LLVM-Backend**: Es ist ein `no_std`-Crate
mit reiner Arithmetik, das der Interpreter auf dem Host genauso benutzt
wie später das Target. Es lässt sich vollständig bauen und prüfen, bevor
eine Zeile LLVM entsteht — und wenn es scheitert, scheitert es an einer
Stelle, an der noch nichts darauf aufbaut.

Der Preis: Der Interpreter ändert sein Verhalten für transzendente
Funktionen. Das ist kein Bruch, sondern die Korrektur eines
undokumentierten Zustands — die Golden-Traces des Korpus müssen einmal neu
erzeugt werden, und der Commit sagt warum.

### 2.2 Wie werden die Vektoren geführt?

| | Rust-Tabellen im Testcode | eigenes Binärformat | **`grammar/libtaktm.md` wie `lexer.md`** |
|---|---|---|---|
| lesbar und prüfbar ohne Werkzeug | nein | nein | ja |
| eine Quelle für Host und Target | ja | ja | ja |
| Konvention im Projekt vorhanden | — | — | ja: `lexer.md`, `mir-format.md`, `trace.md` |
| Aufwand | gering | hoch | gering |

**Entscheidung: `grammar/libtaktm.md` mit demselben Aufbau wie
`grammar/mir-format.md`** — ein Block je Funktion, je Zeile ein Vektor
`f64 sin: <arg als hex> -> <ergebnis als hex>`, geprüft von
`crates/libtaktm/tests/vectors.rs`.

Begründung: Das Projekt hat diese Konvention dreimal, und sie hat sich
bewährt (`lexer.md` fand die L2.2a-Lücke im Python-Tokenizer). Die
Argumente stehen **als Bitmuster**, nicht dezimal — sonst prüft man die
Dezimalkonversion mit, und genau die soll nicht Teil der Zusage sein.

**Die Vektoren sind ein Gate, kein Bericht.** 13.8 sagt: „erst danach
wird eine Funktion in die kuratierte Menge aufgenommen." Eine Funktion
ohne grüne Vektoren ist in `takt-native` nicht sichtbar — der Compiler
lehnt sie ab, wie er heute ein v1.1-Konstrukt ablehnt.

### 2.3 Woher kommen die korrekt gerundeten Werte?

| | eigene Implementierung | CORE-MATH übernehmen | MPFR zur Vektorerzeugung, eigene Implementierung |
|---|---|---|---|
| korrekt gerundet belegbar | schwer | ja (bewiesen) | ja (Referenz ist MPFR) |
| Lizenz und Herkunft sauber | ja | prüfen (MIT) | ja |
| Aufwand | sehr hoch | mittel | mittel |
| TCB-Größe | groß | groß | groß |

**Entscheidung: CORE-MATH als Vorbild und Quelle der Algorithmen, MPFR
als Orakel der Vektorerzeugung.** 4.2 nennt CORE-MATH ausdrücklich als
Ansatz. Die Vektoren entstehen einmalig mit MPFR in hoher Präzision und
werden danach als Datei geführt — die Erzeugung ist ein
Entwicklungswerkzeug, kein Teil des Builds, damit MPFR nicht in die TCB
gerät.

**Randfälle sind Pflichtvektoren**, nicht Kür: ±0, ±∞, NaN, der kleinste
und größte Subnormal, die Argumentreduktionsgrenzen von `sin`/`cos` (π/2
nahe Vielfache), `exp` an der Überlaufgrenze, `pow` mit ganzzahligem
Exponenten. 4.2 verlangt zusätzlich einen Subnormal-Referenzvektor für die
FPU-Prüfung; der gehört in dieselbe Datei.

### 2.3b Wie groß ist die kuratierte Menge am Anfang?

Beim Bauen zeigt sich, was die Schätzung aus Abschnitt 11 verdeckt:
**Korrekt gerundete Transzendente sind keine Zweiwochenarbeit.** Die
CORE-MATH-Implementierung von `pow` allein ist mehrere tausend Zeilen mit
Argumentreduktion in doppelt-doppelter Arithmetik; elf Funktionen in zwei
Breiten sind ein eigenes Projekt.

Zwei Randbedingungen des Projekts schärfen die Frage:

- **Der Workspace hat keine einzige externe Abhängigkeit.** Ein
  `libm`-Crate zu ziehen wäre die naheliegende Abkürzung und würde die
  Linie brechen, die seit M0 gilt.
- **Der Korpus benutzt heute drei Funktionen**: `sin` und `cos` (im
  FOC-Beispiel 11) und `fma` (in 10). Die übrigen acht stehen in keinem
  Programm.

| | alle elf sofort | nur die drei benutzten | **gestufte kuratierte Menge** |
|---|---|---|---|
| Satz 9.4.4 belegbar | ja, aber spät | für die Korpusprogramme | ja, für alles Freigegebene |
| M4 blockiert | Monate | nein | nein |
| ehrlich | ja | nein: `exp` bleibt still falsch | ja: Ablehnung statt stiller Abweichung |
| folgt 13.8 | — | nein | **ja, wörtlich** |

**Entscheidung: Die kuratierte Menge beginnt mit dem, was bewiesen ist,
und wächst. Was nicht darin ist, lehnt der Compiler ab.**

Das ist keine Notlösung, sondern genau die Konstruktion aus 13.8: *„Für
native Funktionen prüft die Suite Bit-Gleichheit der Ergebnisse über alle
Targets …; erst danach wird eine Funktion in die kuratierte Menge
aufgenommen."* Der Satz steht dort über native Funktionen; für die
Mathematik gilt er genauso, weil dieselbe Zusage dahinter steht.

**Stufe 1 (dieser Schritt)** — exakt oder per IEEE-754 garantiert, damit
ohne eigene Approximation korrekt gerundet:

| Funktion | Warum exakt |
|---|---|
| `sqrt` | IEEE-754-Operation, vom Hardwarebefehl korrekt gerundet |
| `fma` | IEEE-754-2008 fusedMultiplyAdd, `mul_add` trägt die Zusage |
| `round`, `floor`, `ceil` | exakt; nur Rundungsmodus, keine Approximation |
| `abs`, `min`, `max` | exakt |

**Stufe 2 (später in M4 oder M5)**: `sin`, `cos` — die beiden, die der
Korpus braucht. Sie sind auch die einfachsten: Argumentreduktion modulo
π/2 mit einer Tabelle, dann ein Minimax-Polynom. Ein Programm, das sie
heute benutzt, bekommt eine Meldung mit Stufe statt eines stillen
Ergebnisses.

**Stufe 3**: `tan`, `asin`, `acos`, `atan`, `atan2`, `exp`, `log`, `pow`.

Der Gewinn ist sofort da, auch ohne Stufe 2 und 3: Satz 9.4.4 gilt ab
Stufe 1 für jedes Programm, das ohne Transzendente auskommt — und das
sind alle Korpusprogramme außer 11. Vor allem aber ist die **Zusage
ehrlich**: Heute rechnet `exp` still mit der Plattform-libm und behauptet
Bitgleichheit; nach diesem Schritt sagt der Compiler, dass er sie nicht
garantieren kann.

### 2.3c `no_std` und `sqrt` schließen sich auf stabilem Rust aus

Beim Bauen von Stufe 1 stellt sich heraus: **`f64::sqrt`, `mul_add`,
`floor` und `ceil` gibt es in `core` nicht**, nur in `std`. Sie sind
LLVM-Intrinsics, und der Weg zu ihnen (`core::intrinsics`) verlangt
Nightly. Der Workspace verbietet ausserdem `unsafe_code`.

Das trifft genau die vier Funktionen, mit denen Stufe 1 beginnt.

| | Software-Emulation in `core` | `unsafe`-Ausnahme | Nightly | **`std`-Feature, Standard an** |
|---|---|---|---|---|
| `sqrt` korrekt gerundet | ja, aber selbst geschrieben | ja | ja | ja: Hardwarebefehl |
| Hardware genutzt (4.2: „in Hardware wo vorhanden") | nein | ja | ja | ja |
| `no_std` bleibt möglich | ja | ja | ja | ja, ohne das Feature |
| Toolchain bleibt stabil | ja | ja | **nein** | ja |
| `unsafe_code = "forbid"` bleibt | ja | **nein** | ja | ja |

**Entscheidung: Das Crate ist `no_std`, ein Feature `std` (Standard an)
liefert die Hardware-Primitiven.** Ohne das Feature meldet Stufe 1
dasselbe [`Unsupported`] wie eine nicht kuratierte Funktion — die
Emulation kommt mit M5, wenn ein echtes `no_std`-Target existiert und
prüfen kann, ob sie nötig ist.

Begründung: 4.2 verlangt für `fma` ausdrücklich „in Hardware wo
vorhanden (VFMA, FMADD, FMA3), sonst korrekt gerundet in Software". Die
Hardware-Variante ist also die gewollte; die Software-Emulation ist der
Rückfall für Targets ohne Befehl. Sie jetzt zu schreiben, hiesse, den
Rückfall vor dem Normalfall zu bauen, ohne ein Target zu haben, an dem
er sich prüfen liesse.

Der Preis ist ehrlich benannt: Ein `no_std`-Bau ohne das Feature hat
heute *keine* kuratierte Funktion. Das ist kein Verlust gegenüber heute
— es gibt noch kein `no_std`-Target —, aber es ist eine Schuld, die M5
einlöst, und `Cargo.toml` sagt es an der Stelle, an der jemand das
Feature abschaltet.

### 2.4 Was `libtaktm` für das Kostenmodell bedeutet

Die Bestandsaufnahme zeigt eine Lücke, die erst mit `libtaktm` sichtbar
wird: `analysis/cost.rs:163` rechnet **jede** Intrinsic gleich —
`class_of(e) + {call: 1}`. Ein `sin` kostet damit so viel wie ein `abs`.

Solange die Mathematik aus der Plattform-libm kommt, ließ sich das nicht
besser machen: Niemand kannte die Kosten. Mit korrekt gerundeten eigenen
Implementierungen ändert sich das — sie sind messbar, und ihre Kosten
gehören in das Budget (9.4.3), das die Schedulability (7.2) und die
Safe-State-Latenz (9.4.5) tragen.

| | gleich lassen | feste Tabelle je Intrinsic | **Tabelle mit Herkunft, kalibriert in M5** |
|---|---|---|---|
| Budget für Regelpfade realistisch | nein: `sin` als O(1) | ja, aber geraten | ja |
| passt zur Linie von `takt size` | — | teils | ja: dieselbe `Origin`-Unterscheidung |
| M4 braucht die Kalibrierung nicht | ja | ja | ja |

**Entscheidung: `libtaktm` liefert je Funktion eine deklarierte
Operationszahl, `cost.rs` liest sie; die Umrechnung in Zeit bleibt M5.**
Das ist dieselbe Konstruktion wie bei nativen Funktionen (4.5: `cost` als
Vertrag) und bei der Latenzrechnung (Ticks exakt, Zeit erst mit
Kalibrierung). Eine Funktion der Bibliothek deklariert ihre Kosten wie
eine native — der Unterschied ist nur, dass sie nicht zur TCB gehört.

Ohne diesen Schritt bliebe ein Budget, das einen PID-Regler mit
`sin`-Aufruf so teuer rechnet wie eine Zuweisung, und die
Schedulability in M6 würde auf dieser Zahl aufbauen.

### 2.5 Wie findet der Codegen seine Abnahme?

Der Exit verlangt „Interpreter ≡ nativ auf Korpus und Fuzzer". Drei
Fassungen sind denkbar:

| | Trace-Vergleich | Zustands-Hash je Tick | **beides, gestuft** |
|---|---|---|---|
| findet Abweichung | am Output | an der Ursache | ja |
| Diagnose bei Fehlschlag | „Tick 412 anders" | „σ nach Tick 412 anders, Variable x" | präzise |
| Kosten | gering | mittel | mittel |

**Entscheidung: Trace-Vergleich als Abnahme, Zustands-Hash als
Diagnosewerkzeug.** Der Trace ist die semantische Zusage (9.4.4 spricht
von Outputs); der Zustands-Hash ist das, was einen Fehlschlag in Minuten
statt Stunden erklärt. Er steht hinter einem Flag und gehört nicht in die
Abnahme, damit die Zusage nicht stillschweigend verschärft wird.

### 2.6 Wie weit geht die Runtime in M4?

Die Meilensteintabelle nennt `takt-rt-core` + `linux_rt`. Offen ist, ob
die Profile `rtos` und `boot` schon Platzhalter bekommen.

**Entscheidung: `takt-rt-core` trägt die vier Profile als Aufsatzpunkte,
gebaut wird nur `linux_rt`.** Dieselbe Linie wie bei der MIR in M0:
Die Struktur ist für die volle Referenz entworfen, implementiert ist, was
der Meilenstein braucht. Ein `rtos`-Aufsatz, der später eingezogen wird,
bräche sonst den Kern.

### 2.7 Bleibt der Interpreter?

Ja, und das ist eine Entscheidung, keine Trägheit. Nach M4 gibt es zwei
Implementierungen derselben Semantik. Der Plan nennt in Prinzip 2 den
Interpreter als Spezifikation; ihn nach dem Codegen abzuschaffen hieße,
das Orakel wegzuwerfen, mit dem jede spätere Sprachfrage entschieden wird
— und C3 aus `plan/ideen-bewertung.md` (Schatten-Interpreter als
Diversitätsargument, v2/v3) setzt ihn voraus.

---

## 3. `libtaktm`: Umfang und Aufbau

### 3.1 Was hinein gehört

| Gruppe | Funktionen | Warum |
|---|---|---|
| Wurzel | `sqrt` | IEEE-Befehl; als Vektor geführt, um die FPU-Einstellung zu prüfen |
| Trigonometrie | `sin`, `cos`, `tan`, `asin`, `acos`, `atan`, `atan2` | 4.2 nennt sie |
| Exponential | `exp`, `log`, `pow` | 4.2 nennt sie |
| Fused | `fma` | IEEE-754-2008, korrekt gerundet (4.2) |
| Rundung | `round`, `floor`, `ceil` | targetunabhängig, aber Vektoren belegen die Fault-Grenzen (4.1) |

Beide Breiten: `f32` und `f64`. Die `f32`-Fassung ist **nicht** die
gerundete `f64`-Fassung — das wäre doppelt gerundet und damit nicht
korrekt gerundet. Sie bekommt eigene Vektoren.

Nicht hinein gehören die 15 Integer-Intrinsics (`rotl`, `wrapping_*`,
`saturating_*`, `min`, `max`, `abs`, `interp`): Sie sind exakt definiert
und brauchen keine Bibliothek.

### 3.2 Schnittstelle

```rust
// crates/libtaktm/src/lib.rs — no_std, keine Allokation, keine Panics
pub fn sin_f64(x: f64) -> f64;
pub fn sin_f32(x: f32) -> f32;
…
```

Flach, ohne Trait, ohne Generics: Der Interpreter ruft sie auf dem Host,
der Codegen erzeugt Aufrufe an dieselben Symbole auf dem Target. Ein
Trait brächte einen dynamischen oder monomorphisierten Zwischenschritt,
den keine der beiden Seiten braucht.

### 3.3 Panic-Freiheit

13.8 verlangt „Panic-Freiheit unter Fuzzing". Zwei Mittel:

1. `#![no_std]` plus `panic = "abort"` im Testprofil — ein Panic ist dann
   ein Testfehler, kein Unwind.
2. Ein Fuzzer über alle Bitmuster-Klassen je Funktion (Zufall plus die
   Randfälle aus 2.3), der nur prüft, dass nichts panickt und jedes
   Ergebnis endlich oder ein dokumentiertes NaN ist.

Der Fuzzer läuft in CI, nicht bei jedem `cargo test`.

---

## 4. `takt-llvm`: Aufbau

### 4.1 Wie kommt LLVM ins Projekt?

| | `inkwell` (LLVM-Bindings) | **eigener Textausgeber (`.ll`)** |
|---|---|---|
| strikte FP steuerbar | ja (Attribute je Instruktion) | ja, von Hand — aber als Abwesenheit von Flags vollständig testbar |
| Build-Abhängigkeit des Compilers | LLVM-Installation | **keine** |
| Fehler früh sichtbar | ja (typisiert) | beim Assemblieren, in CI |
| Artefakt für die Qualifikation (13.4) | nein (Graph im Speicher) | **ja (Textdatei)** |
| reproduzierbare Builds (11.3) belegbar | über das Binary | **über den Diff der IR** |
| an eine LLVM-Hauptversion gebunden | ja | nein |
| Debug-Info (12.4) | ja | aufwendig (`!DILocation` von Hand) |

**Entscheidung (revidiert 2026-09-11): eigener Textausgeber (`.ll`),
LLVM als Werkzeug statt als Bibliothek.**

Die erste Fassung dieses Abschnitts entschied sich für `inkwell`, weil
die strikte FP-Semantik an Instruktionsattributen hängt, die von Hand
geschrieben fehleranfällig sind. Das Argument ist richtig, aber es ist
das schwächste der vier — und es ist das einzige, das sich mechanisch
auffangen lässt. Drei Gründe wiegen schwerer:

**1. Die Zertifizierung (13.4) verlangt Nachvollziehbarkeit.** Das Ziel
ist ein *qualifizierbarer* Codegen nach dem Vorbild von SCADE. In einer
Qualifikation muss belegt werden, was der Compiler erzeugt hat. Ein
Textartefakt ist prüfbar, diffbar und archivierbar; `inkwell` baut einen
Graphen im Speicher, den hinterher niemand ansieht. Die Architekturlinie
des Plans — „kleine MIR, Referenzinterpreter, differentielles Testen" —
ist eine Linie der Einsehbarkeit, und Text setzt sie fort.

**2. Reproduzierbare Builds (11.3) werden einfacher.** Verlangt sind
bitidentische Binaries mit deterministischer Symbolordnung. Mit
Textausgabe ist der Beitrag des Compilers eine Datei, die sich hashen und
vergleichen lässt; ein Unterschied ist ein Diff statt einer Vermutung
über zwei Binaries.

**3. Die Linie „keine externen Abhängigkeiten" bleibt.** `inkwell` bindet
den Workspace an eine LLVM-Hauptversion, und jedes Upgrade wird eine
Migration. Textuelle IR ist ein stabiles, dokumentiertes Format. Die
LLVM-Version wird in beiden Fällen gepinnt und steht im Lauf-Header
(11.3).

**Das FP-Risiko wird getestet, nicht wegdiskutiert.** Strikte FP heißt
in der Textform die *Abwesenheit* von Flags — und Abwesenheit ist
vollständig prüfbar: Ein Test durchsucht die erzeugte IR auf `fast`,
`nnan`, `ninf`, `nsz`, `arcp`, `contract` und `reassoc` und schlägt bei
jedem Vorkommen fehl. Abschnitt 8 nennt genau diesen Test ohnehin als
Gegenmaßnahme; mit `inkwell` wäre er schwerer zu schreiben, nicht
leichter.

**LLVM wird trotzdem gebraucht**, nur an anderer Stelle: Schritt 8
(differentielles Testen) ist die Abnahme von M4 und muss den erzeugten
Code *ausführen*. Die Trennung ist der eigentliche Gewinn — der Compiler
ist überall baubar und testbar, die Abnahme läuft dort, wo die
Toolchain steht.

### 4.2 Was der Codegen aus M3 erbt

Drei Dinge, die M3 gebaut hat und die der Codegen benutzt statt sie neu
zu rechnen:

- **`Expr::repr`** (I32/I64) — die Darstellungsverengung. Der Codegen
  wählt die LLVM-Breite danach; ohne sie wäre alles i64.
- **`Machine::budget`** — geht in den Lauf-Header und später in die
  Schedulability.
- **Die bewiesenen Intervalle** (`Expr::range`) — wo M3 einen `Checked`
  entfernt hat, erzeugt der Codegen keinen Zweig.

Das ist die Begründung der Reihenfolge M3 → M4 aus dem Plan, jetzt
konkret.

### 4.3 Die Lowering-Skizze steht

11.2 beschreibt sie bereits: `struct hotfire_state { conf, t_in_state,
vars, state_vars, blocks, every_next, cur, … }` plus `fn hotfire_step(s,
i, psi, o)`. Der Codegen setzt sie um, er erfindet sie nicht. `SEM-11.2`
steht auf `offen` und wird mit diesem Schritt `fertig`.

---

## 5. Runtime und HAL

### 5.1 Die Tickschleife steht in 12.1

Dreizehn Schritte, wörtlich abzubilden — von `wait_for_tick_boundary()`
bis `maybe_sleep()`. `takt-rt-core` implementiert sie `no_std` und ohne
Allokation; `linux_rt` liefert Tickquelle (Timer-FD), Treiber-Threads und
Telemetrie.

### 5.2 Treiber sind Traits

Prinzip 4: „Treiber als Traits, deren Simulationsimplementierung der
Sim-Backend ist (die Sim/HW-Umschaltung ist ein Treiberwechsel, kein
zweiter Programmpfad)."

**Folge für M4: Der Simulationstreiber ist die erste
HAL-Implementierung, nicht ein Testdouble.** Damit ist `takt sim` nach M4
derselbe Programmpfad wie `takt run`, nur mit anderem Treiber — und der
Golden-Korpus aus M1/M2 prüft beide.

### 5.3 Rand-Selbstprüfungen (12.6)

Der defensive Treiberrand ist bereits spezifiziert: Zeitstempel, `seq`,
Kapazitäten, Qualitätsflags, Ranges, `max_slew`. Er gehört in
`takt-hal`, nicht in jeden Treiber — sonst steht er dreimal verschieden
da.

---

## 6. Record und Replay

12.5 nennt den Inhalt einer Aufzeichnung: Logik-Hash, Bindungen,
Laufzeitprofil, Profil und Parametervektor, `s0` der `persist`-Variablen,
alle Input-Snapshots mit Zeitstempeln.

**Entscheidung: Das Trace-Format aus `grammar/trace.md` trägt auch die
Aufzeichnung, mit einem Kopf.** Es ist zeilenorientiert, kanonisch und
hat Vektoren; ein zweites Format brächte einen zweiten Leser. Der Kopf
ist der Lauf-Header (`FMT-Lauf-Header`, mit M4 fällig).

Damit ist auch A3 aus `plan/ideen-bewertung.md` vorbereitet: Die
Hashkette `h_k = H(h_{k-1} ‖ I_k ‖ O_k)` setzt auf der kanonischen
Trace-Zeile auf. Gebaut wird sie in M6; M4 muss ihr nur nicht im Weg
stehen.

---

## 7. Schritte

| # | Schritt | Ergebnis |
|---|---|---|
| 1 | `libtaktm` mit Vektoren, `grammar/libtaktm.md` | Satz 9.4.4 wird für transzendente Funktionen belegbar |
| 2 | Interpreter auf `libtaktm` umstellen (zwei Closures plus vier Arme); Golden-Traces neu | ein reproduzierbares Orakel |
| 3 | `takt-hal`: Traits, Rand-Selbstprüfungen, Simulationstreiber | `takt sim` läuft über den HAL |
| 4 | `takt-rt-core`: Tickschleife nach 12.1, vier Profilpunkte | Runtime ohne Codegen testbar (gegen den Interpreter) |
| 5 | `takt-rt-linux`: Tickquelle, Threads, Telemetrie | `linux_rt` läuft |
| 6 | `takt-llvm`: Gerüst, Typen, Ausdrücke, strikte FP | erste MIR-Fixtures übersetzen |
| 7 | `takt-llvm`: Maschinen, Zustände, Tickschritt (11.2) | 14.1 läuft nativ |
| 8 | Differentielles Testen: Korpus, dann Fuzzer | Interpreter ≡ nativ |
| 9 | `takt-native`: die kuratierte Menge, Prelude-Deklarationen, `call_native` | Natives laufen statt zu trappen |
| 10 | Record/Replay; `takt run`, `takt replay` | Läufe bitgenau reproduzierbar |
| 11 | aarch64 als zweites Target | x86-64 ≡ aarch64 |

Schritte 1–5 brauchen kein LLVM und können parallel zu 6–7 laufen, sobald
1 und 2 stehen. Schritt 8 ist die Abnahme; 11 schärft sie.

Schritt 9 steht bewusst hinter der Abnahme: Native Funktionen sind heute
ein Trap mit der Meldung „noch nicht verfuegbar (M4)", und 13.8 verlangt
für jede von ihnen Bit-Gleichheit über alle Targets plus Panic-Freiheit
unter Fuzzing — dieselbe Hürde wie für `libtaktm`. Sie erst zu bauen,
wenn das differentielle Testen läuft, heißt: Die Hürde ist schon
aufgebaut, wenn die erste Funktion sie nehmen muss.

---

## 8. Risiken

| Risiko | Gegenmaßnahme |
|---|---|
| **Korrekt gerundete Transzendente sind aufwendiger als geschätzt** | Schritt 1 zuerst; scheitert es, ist nur `libtaktm` betroffen, nicht der Codegen. Rückfallposition: die kuratierte Menge zunächst auf `sqrt`, `fma` und die Rundungen beschränken — 4.2 erlaubt das, weil eine Funktion ohne grüne Vektoren gar nicht erst aufgenommen wird (13.8) |
| LLVM-Version bricht Reproduzierbarkeit | Version pinnen, im Lauf-Header führen (11.3); Upgrade ist eine Entscheidung mit erneuter Abnahme |
| Strikte FP wird von LLVM stillschweigend gelockert | je Instruktion prüfen, nicht global; ein Test, der die erzeugte IR auf Fast-Math-Flags absucht |
| PREEMPT_RT-Jitter | ab Schritt 5 messen; `Overrun` als Fault früh sichtbar (Plan, Abschnitt 7) |
| Zwei Semantiken driften auseinander | Schritt 8 ist die Abnahme, nicht der Abschluss: Der differentielle Test läuft ab da in CI |

---

## 9. Was M4 nicht tut

- **Kein `takt test`, kein `scenario`.** Szenarien sind v1.1 (M6); `takt
  sim --golden` deckt die Abnahme von M1/M2 weiter ab.
- **Keine Kalibrierung.** `c_target`, `guard`, `jitter` kommen mit M5
  (`takt bench`); die vier Prüfungen, die darauf warten, bleiben in M6.
- **Kein `no_std`-Target.** `baremetal` ist M5. `takt-rt-core` ist
  trotzdem `no_std` geschrieben, damit M5 nur einen Aufsatz braucht.
- **Keine Hashkette, keine Signatur.** A3 ist M6; M4 hält das Format
  offen.

---

## 10. Wirkung auf die Inventur

| Gruppe | heute | nach M4 |
|---|---|---|
| 50 M4-Einträge | 47 offen, 3 teilweise | `fertig` |
| `SEM-11.2` (Lowering-Skizze) | offen | fertig |
| `THM-Satz-9.4.4` (Sim = HW) | offen | **fertig** — der Satz bekommt seinen Test |
| `THM-Satz-9.4.3` | offen | fertig |
| `FMT-Aufzeichnung`, `FMT-Lauf-Header` | offen | fertig |
| `CLI-run`, `CLI-replay`, `CLI-graph` | offen | fertig |
| `CLI-sim` | fertig | bleibt, läuft dann über den HAL |
| `LIB-fma`, `LIB-sin_fast` | offen | fertig |

Satz 9.4.4 ist der eigentliche Ertrag: Er steht seit M0 in der Referenz
und ist die Zusage, auf der Simulation, Zertifizierung und der
Schatten-Interpreter (v2/v3) aufbauen. M4 ist der Meilenstein, der ihn
prüfbar macht — und Schritt 1 ist die Bedingung dafür.

---

## 11. Größenordnung

Der Plan veranschlagt M4 mit 8–10 Wochen bei drei bis vier Ingenieuren.
Die Aufteilung nach diesem Entwurf:

| Schritt | Anteil | Bemerkung |
|---|---|---|
| 1–2 `libtaktm` und Umstellung | ~2 Wochen | die 11 Funktionen in zwei Breiten, plus Vektoren |
| 3–5 HAL und Runtime | ~2 Wochen | die Tickschleife steht in 12.1 wörtlich |
| 6–7 Codegen | ~3 Wochen | der größte Block; die Lowering-Skizze steht in 11.2 |
| 8 differentielles Testen | ~1 Woche | Korpus und Golden-Traces existieren |
| 9–11 Natives, Replay, aarch64 | ~2 Wochen | |

Die Unsicherheit liegt fast vollständig in Schritt 1: Korrekt gerundete
Transzendente sind bekannt schwierig, und die Schätzung von zwei Wochen
gilt nur, wenn CORE-MATH als Quelle brauchbar ist. Scheitert das, greift
die Rückfallposition aus Abschnitt 8 — die kuratierte Menge beginnt klein
und wächst, und der Rest von M4 bleibt davon unberührt.

Das ist zugleich der Grund für die Reihenfolge: Der teuerste und
unsicherste Schritt steht vorn, wo sein Scheitern am wenigsten kostet.

---

## 12. Stand der Umsetzung

| Schritt | Stand |
|---|---|
| 1 `libtaktm` mit Vektoren | **fertig** |
| 2 Interpreter umgestellt | **fertig** |
| 3 `takt-hal` mit dem defensiven Rand | **fertig** |
| 4 `takt-rt-core`: die Tickschleife | **fertig** |
| 5 `takt-rt-linux`: Tickquelle und Zeitgarantie | **fertig** |
| 6 `takt-llvm`: Gerüst, Typen, Ausdrücke | **fertig** |
| 7 `takt-llvm`: Maschinen, Zustaende, Tickschritt | **fertig** |
| 8 Differentielles Testen (Korpus und Fuzzer) | **fertig** |
| 9 `takt-native`: die kuratierte Menge | **fertig** |
| 10 Record/Replay, `takt run`, `takt replay` | **fertig** |
| 11 aarch64 als zweites Ziel | **fertig** |

### 12.1 Was Schritt 1 und 2 ergeben haben

**Die Zusage ist jetzt ehrlich.** Vorher rechnete `exp` still mit der
Plattform-libm und behauptete damit Bitgleichheit über Targets. Jetzt
sagt der Compiler vor dem Lauf, dass er sie für diese Funktion nicht
geben kann:

```
error[SC-3]: `sin` ist noch nicht korrekt gerundet implementiert (4.2)
  = Vorschlag: bis dahin nicht benutzbar: eine Zahl aus der
    Plattformbibliothek waere auf einem anderen Target eine andere
    (Satz 9.4.4)
```

Das kostet: `corpus-try/11_foc_drive.takt` benutzt `sin` und `cos` und
übersetzt nicht mehr. Das ist kein Rückschritt, sondern der sichtbar
gemachte Zustand — die Datei lief vorher mit einem Ergebnis, das auf
einem anderen Target ein anderes gewesen wäre. Tokenizer, Parser,
Formatter und der differenzielle Parser sind unberührt; nur die
Semantikstufe lehnt ab, und genau dort gehört es hin.

**Die Vektoren sind an drei Stellen gesichert**, nicht nur einer:

1. `every_vector_holds` — jede der 120 Zeilen wird nachgerechnet.
2. `every_curated_function_has_vectors` — keine Funktion gilt als
   kuratiert, ohne dass Vektoren für beide Breiten dastehen.
3. `no_vector_for_an_uncurated_function` — und umgekehrt keine Zeile für
   eine Funktion, die der Compiler ablehnt. Sonst behauptete die
   Spezifikation eine Zusage, die er nicht gibt.

Der dritte Test ist der, an den man beim Schreiben nicht denkt, und der
die Spezifikation ehrlich hält, wenn jemand später eine Funktion aus der
kuratierten Menge nimmt.

### 12.2 Zwei Befunde beim Bauen

**`no_std` und `sqrt` schließen sich auf stabilem Rust aus** (2.3c).
Gelöst mit einem Feature `std`, Standard an; ohne es ist keine Funktion
der Stufe 1 kuratiert, und `curated()` sagt das auch — sonst meldete der
Compiler eine Funktion als verfügbar, die beim Aufruf fehlt.

**Die Vektorerzeugung stolperte über das Vorzeichen der Null.** Pythons
`math.trunc(-0.5)` liefert die ganze Zahl `0`, Rust und IEEE-754 liefern
`-0.0`. Der erste Vektorsatz war an vier Stellen falsch — gefunden vom
Test, nicht von der Durchsicht. Das ist das Argument für Bitmuster statt
Dezimalzahlen in einem Satz.

### 12.3 Was Schritt 3 ergeben hat

**Der Befund: `max_slew` und `debounce` waren tote Deklarationen.** Beide
Attribute ließen sich seit M0 schreiben, sie kamen durch Parser, Sema und
MIR-Roundtrip — und **niemand las sie je**. Ein Programm mit
`with max_slew = 50 bar/s` verhielt sich exakt wie eines ohne. Das ist
schlimmer als eine fehlende Funktion: Die Deklaration sah aus wie eine
Zusage, und 10.2 zählt beide unter den Annahmen auf, die der Beweiser
„ohne Zusatzaufwand voraussetzen" darf, *weil* der Treiberrand sie zur
Laufzeit erzwingt. Er tat es nicht.

Dass die Prüfungen 3 und 4 in derselben Tabelle stehen, aber nur 3
implementiert war, hat das verdeckt: `enforce_range` gab es, und es sah
nach dem Rand aus.

**Der Rand steht jetzt in `takt-hal` und nicht im Treiber.** 5.3 hatte das
schon entschieden; beim Bauen zeigte sich der zweite Grund dafür. Die
Prüfungen 3 und 4 brauchen einen Zustand *je Input* — den letzten guten
Wert, als Bezugspunkt von `max_slew` und als das, was `debounce` hält. Läge
er im Treiber, hätte jeder Treiber seine eigene Entprellung, und zwei
Treiber desselben Programms entprellten verschieden.

**`Scalar` statt `Value`.** Der Rand rechnet nicht mit der
Wertdarstellung des Interpreters, sondern mit einem Trait, das nur Ordnung
und Differenz verlangt. Der Grund ist Satz 9.4.4: Der erzeugte Code hat
getypte Strukturen (11.2), keinen `Value`. Ein Rand über `Value` wäre im
Codegen nachgebaut worden — und ein nachgebauter Rand ist ein zweiter
Rand, der in den Randfällen abweicht, also genau dort, wo er zählt.

Ein Detail, das beim Schreiben auffiel: Der Rand kann den gehaltenen Wert
nicht selbst führen, weil er den Typ des Kanals nicht kennt. Ein
`int`-Kanal bekäme aus der Entprellung sonst einen `f64`. `Verdict.held`
sagt darum nur *dass* gehalten wird; **was**, weiß der Aufrufer, der
geliefert hat.

**Der Simulationstreiber ist eine Implementierung der Traits**, wie 5.2 es
verlangt — kein Testdouble. `takt sim` läuft damit durch denselben Rand,
den später `takt run` benutzt.

**Was der neue Korpusfall zeigt.** `corpus-try/sim/14_8` führt zwei
Kanäle mit demselben Sprung: `raw_p` ohne `debounce`, `chamber_p` mit
`debounce = 2`. Der Golden-Trace hält den Unterschied fest:

```
t=200 alert edge_watch on "chamber_p entprellt: IMPLAUSIBLE"
t=200 alert edge_watch on "raw_p ungueltig: IMPLAUSIBLE"
t=200 out p_quality 1        # Suspect: der letzte gute Wert wird gehalten
t=400 out p_quality 2        # nach `debounce` Lieferungen: Bad
t=500 out p_quality 0        # Erholung ist automatisch
```

Das ist Entscheidung 18 in einem Bild: Die Entschärfung ist explizit, ihre
Haltedauer beschränkt und im Trace sichtbar — ein Sicherheitsargument kann
sie einrechnen.

**Was Schritt 3 noch nicht tut.** Die Prüfungen 1, 2, 6 und 7 stehen mit
Tests im Crate, werden aber erst von der Tickschleife gerufen; sie stehen
in der Inventur darum auf `teilweise`, nicht auf `fertig`. Prüfung 5
(`decode`) braucht den Aufrufer, der den Record kennt. Das ist Schritt 4:
`takt-rt-core` ruft `validate_and_bound()` an der Stelle, die 12.1 dafür
nennt.

### 12.4 Was Schritt 4 ergeben hat

**Die Schleife aus 12.1 hat dreizehn Schritte, aber nur vier gehören in
den Kern.** Neun davon sind Semantik und stehen dort, wo die Semantik
steht — im Interpreter oder später im erzeugten Code. `takt-rt-core`
nimmt die vier, die *Zeit* sind: `wait_for_tick_boundary()`,
`record_and_telemeter()`, `kick_watchdog()`, `maybe_sleep()`.

Das war die entscheidende Abgrenzung. Ein Kern, der auch die Semantik
führt, müsste generisch über die Wertdarstellung sein — der Interpreter
rechnet mit `Value`, der erzeugte Code mit getypten Strukturen (11.2).
Der Kern ruft darum `Program::tick` und weiß nicht, was dahinter liegt.

**Der Kern hat keine einzige Abhängigkeit, auch nicht `takt-mir`.** Das
ist keine Sparsamkeit, sondern die Bedingung für M5: Was auf der MCU
läuft, darf die IR nicht kennen. Was das Programm der Schleife mitteilen
muss — T₀, das Profil, die Policy — sind Zahlen.

**Ein Befund, den erst ein Test zutage gefördert hat.** Die
Überlaufmessung beantwortete zwei Fragen mit einem `bool`: „war es ein
Überlauf?" und „folgt daraus ein Fault?". 7.3 trennt sie aber: Die
physische Verzögerung wird *immer* protokolliert, nur der Fault hängt an
der Policy. Unter `alert` — dem Modus für unkritische Systeme — wäre der
Überlauf damit vollständig aus dem Trace verschwunden, also genau dort,
wo er die einzige Auskunft ist, die man noch bekommt. Der Test
`the_alert_policy_raises_no_fault` hat es gefunden, nicht die Durchsicht.

**Drei Regeln, die die Reihenfolge festlegen und die Tests festhalten:**

1. **Die logische Zeit folgt nicht der Uhr.** `now` ist ein Vielfaches
   von T₀, auch nach einem Überlauf. Ohne diese Trennung hinge der Trace
   an der Hardware, und Satz 9.4.1 gälte auf der Box nicht mehr.
2. **Deadlines sind absolut** (12.2). Die nächste Frist wächst um T₀, nie
   um die gemessene Dauer — sonst pflanzt ein zu später Tick sich fort.
3. **Der Watchdog wird nach dem Schritt bestätigt**, nicht davor. Davor
   bestätigte er einen Tick, der noch nicht durchgelaufen ist.

**Die vier Profile sind Aufsatzpunkte, keine Platzhalter** (2.6). Der
Unterschied ist praktisch: `boot` trägt heute schon `may_sleep = false`,
weil 12.8 es sagt, und ein Test hält es fest. Gebaut wird in M4 nur
`linux_rt`.

**Was Schritt 4 noch nicht tut.** Die Schleife läuft gegen eine Testuhr,
nicht gegen einen Timer-FD, und hinter `Program::tick` steht noch kein
Programm. Beides ist Schritt 5.

### 12.5 Was Schritt 6 ergeben hat

**Die Entscheidung aus 4.1 wurde revidiert: Textausgabe statt
`inkwell`.** Der Anlass war praktisch — auf der Entwicklungsmaschine ist
kein LLVM installiert, und `inkwell` ließe sich dort weder bauen noch
testen. Die Prüfung der Gründe ergab aber, dass Text auch langfristig die
bessere Wahl ist; die Begründung steht jetzt in 4.1. Kurz: Die
Zertifizierung (13.4) verlangt ein prüfbares Artefakt, reproduzierbare
Builds (11.3) werden über einen Diff belegbar statt über zwei Binaries,
und der Compiler bleibt ohne LLVM-Installation baubar. LLVM wird zum
Werkzeug der Abnahme, nicht zur Bibliothek des Compilers.

**Strikte FP ist in der Textform eine Abwesenheit — und darum
vollständig prüfbar.** Das war das einzige starke Gegenargument, und es
kehrt sich um: `emit` bietet *keine* Methode, die ein Flag setzen könnte,
und zwei Tests durchsuchen die erzeugte IR nach allen sieben (`fast`,
`nnan`, `ninf`, `nsz`, `arcp`, `contract`, `reassoc`). Mit `inkwell`
wäre dieser Test schwerer zu schreiben, nicht leichter.

**Drei Stellen, an denen die Referenz die Instruktion bestimmt:**

1. **Keine `nsw`/`nuw` auf Ganzzahlarithmetik.** Sie erklären den Überlauf
   für undefiniert — und dann dürfte LLVM genau die Prüfung
   wegoptimieren, die ihn nach 4.1 zu einem Fault machen soll. Ein Test
   hält es fest.
2. **Fließkommaliterale stehen als Bitmuster**, nie dezimal. `0.1`
   dezimal wäre eine andere Zahl als die im Programm, und die Abweichung
   fände niemand — dieselbe Lehre wie bei den `libtaktm`-Vektoren (12.2).
3. **Geordnete Vergleiche** (`olt` statt `ult`). NaN kann in Takt nicht
   entstehen (4.1), aber die geordnete Form bleibt auch ohne diese Zusage
   richtig.

**Was nicht abgebildet ist, sagt das.** `ty::lower` und `expr::lower`
liefern für einen unbekannten Typ oder Knoten `None` beziehungsweise
`NotYet` statt still etwas anderes zu erzeugen — dieselbe Linie wie bei
der kuratierten Menge von `libtaktm` (13.8): lieber eine Meldung als ein
Ergebnis, dem man nicht ansieht, dass es falsch ist. Zwei Tests prüfen
genau das.

**Was Schritt 6 noch nicht tut.** `Checked`-Knoten senken heute nur ihren
Wert; der Fault-Trampolin, in den sie springen, entsteht mit der Maschine
(Schritt 7). Ein Zweig ohne Ziel wäre schlechter als keiner. Variablen
stehen hinter dem Trait `Vars`, weil ihr Speicherort der Zustands-Struct
aus 11.2 ist — auch er kommt mit Schritt 7.

### 12.6 LLVM ist da — und hat sofort einen Fehler gefunden

Mit der Installation von LLVM (clang 22.1.8) ist die Textausgabe
nachprüfbar geworden. `crates/takt-llvm/src/toolchain.rs` sucht `clang`,
`tests/against_llvm.rs` übersetzt die erzeugte IR damit.

**Die Distribution liefert kein `llc`.** Das winget-Paket `LLVM.LLVM`
enthält die Clang-Werkzeuge, nicht die LLVM-Entwicklerwerkzeuge — `llc`,
`opt`, `llvm-as` und `lli` fehlen. Das ist kein Hindernis: `clang -c`
assembliert wie `llc`, `-O2` optimiert wie `opt`, und clang linkt
außerdem. Ein Werkzeug, das überall vorhanden ist, ist einem
vorzuziehen, das man nachinstallieren muss — die Suche in `toolchain`
zielt darum auf `clang`, nicht auf `llc`.

**Vier Zusagen sind jetzt belegt statt behauptet:**

1. **Die IR assembliert.** Vorher prüften die Tests Text, der richtig
   *aussah*.
2. **LLVM liest die Bitmuster als die gemeinten Zahlen** — geprüft für
   `0.1`, `-0.0`, den kleinsten Subnormal und den größten endlichen Wert.
   Der Test lässt LLVM die Zahl ausgeben und vergleicht Bitmuster.
3. **LLVM kontrahiert `a * b + c` nicht zu einem `fma`.** Das ist die
   zentrale Zusage aus 4.2 („was man schreibt, bekommt man"), und sie
   war bis hierher eine Annahme: Ob LLVM *trotz* fehlender Flags
   kontrahiert, sagt nur LLVM. Der Testfall ist so gewählt, dass sich
   beide Wege im letzten Bit unterscheiden.
4. **Dieselbe IR ergibt dieselbe Objektdatei** (11.3).

**Der Fehler, den nur clang finden konnte.** Die erste Fassung der
Maschinen-IR fiel von einem Basisblock in die nächste Marke, ohne
Terminator. LLVM verlangt, dass jeder Block mit `br`, `ret`, `switch`
oder `unreachable` endet; ein Durchfallen ist in der Textform gar nicht
ausdrückbar. Kein Textest hätte das gefunden — er prüft, was dasteht,
nicht was fehlt. `emit` führt jetzt Buch, und `label()` ergänzt den
Sprung, weil „weiter mit" in LLVM diese Form hat.

Das ist zugleich die Antwort auf den Einwand aus 4.1, der Textausgeber
sei fehleranfälliger: Er ist es — und die Gegenmaßnahme ist nicht
Sorgfalt, sondern LLVM selbst im Test.

### 12.7 Was Schritt 7 ergeben hat

**Der Zustands-Struct steht, wie 11.2 ihn beschreibt**, und er steht an
*einer* Stelle: `state_struct()` ist die einzige Quelle für die
Feldreihenfolge. Zwei Stellen, die sie unabhängig ausrechnen, wären zwei
Gelegenheiten, sie verschieden auszurechnen — dieselbe Überlegung wie
beim `wire_size`-Plan eines Records im Sema.

**`conf` ist ein Pfad-Array fester Tiefe**, kein Zeiger und keine
Rekursion. Damit ist der Zustandsraum statisch bekannt, und das ist die
Bedingung dafür, dass `takt size` (11.5) rechnen kann und dass kein Heap
nötig ist (13.4, Power of Ten).

**Der Fault-Trampolin merkt vor, er führt nicht aus.** 5.4 lässt den
Fault in der Abort-Phase wirken, damit die Reihenfolge der Maschinen
keine Rolle spielt (Satz 9.4.1) — der Trampolin setzt darum das
`pending`-Flag und verlässt die Schrittfunktion.

**Die Tests übersetzen echte Korpusprogramme**, keine erfundene MIR: Ein
Struct, der nur für Testdaten stimmt, ist kein Beleg. Sieben
Korpusdateien übersetzen heute fehlerfrei zu MIR; die Maschinen-IR von
dreien geht durch clang.

### 12.8 Der Tickschritt läuft

**Vier Korpusprogramme übersetzen zu echtem x86-64-Maschinencode.**
`01_minimal`, `03_sequences_and_faults`, `13_framing` und `14_latency`
gehen durch clang; `llvm-objdump` zeigt die Instruktionen. Das ist der
erste Punkt in M4, an dem ein Takt-Programm nativ vorliegt.

Die Schrittfunktion hat die Form aus 11.2:

```llvm
define void @tank_guard_step(ptr %0, ptr %1, ptr %2, ptr %3) {
  %7 = load i8, ptr %6                      ; conf[0]
  switch i8 %7, label %ende [ i8 0, label %tank_guard_IDLE … ]
tank_guard_WATCH:
  %9  = load double, ptr %8                 ; tank_p aus dem Prozessabbild
  %11 = load double, ptr %10                ; LIMIT aus dem Parametervektor
  %12 = fcmp olt double %9, %11             ; check tank_p < LIMIT
  br i1 %12, label %weiter1, label %fault_tank_guard
…
fault_tank_guard:
  store i1 true, ptr %14                    ; pending vormerken (5.4)
  ret void
}
```

**Der `check` ist die interessanteste Zeile.** Die Bedingung ist die
*gute* Seite, der Sprung geht bei `false` in den Trampolin, und der
Trampolin *merkt vor statt auszuführen* — 5.4 lässt den Fault in der
Abort-Phase wirken, damit die Reihenfolge der Maschinen keine Rolle
spielt (Satz 9.4.1).

**Drei Entscheidungen beim Senken:**

1. **Variablen liegen im Speicher, nicht in Registern.** Das sieht teuer
   aus und ist es nicht: LLVM hebt mit `mem2reg` heraus, was nicht im
   Speicher bleiben muss. Der Codegen wäre dabei schlechter als LLVM und
   müsste seine Entscheidung gegen jede Optimierung verteidigen.
2. **`a if c else b` wird `select`, nicht ein Sprung.** Beide Seiten sind
   total (4.1), es gibt keinen Seiteneffekt zu sparen, und `select` bleibt
   ein Basisblock. Wo eine Seite faulten kann, steht in der MIR ein
   `Checked` *darin*, und das erzeugt seinen Zweig selbst.
3. **Der Default-Zweig des `switch` geht ans Ende, nicht nach
   `unreachable`.** Eine Konfiguration außerhalb der Blätter kann nicht
   entstehen, aber `unreachable` wäre die schärfere, unbelegte Aussage —
   4.1 verlangt Totalität, nicht undefiniertes Verhalten.

**Ein Befund über Fehlermeldungen.** Die erste Fassung meldete nur
„Ausdruck", wenn ein Knoten nicht gesenkt wurde. Damit ließ sich nicht
arbeiten: Genau danach fragt, wer den Codegen weiterbaut. `node_name()`
nennt jetzt den Knoten — aus „Ausdruck" wurde „Feldzugriff" und „Zugriff
(`.valid`, `.age`, …)", und beides sagt sofort, was als Nächstes fehlt.

**Eine abgebrochene Funktion wird verworfen, nicht halb geschrieben.**
Trifft der Codegen auf etwas, das er nicht senkt, schneidet er die
angefangene Funktion wieder ab (`Module::abort`). Eine halbe
Schrittfunktion wäre schlimmer als keine, weil sie möglicherweise
übersetzt. Ein Test zählt `define` gegen `}`.

**Was noch fehlt**, und die Meldungen sagen es genau: Feldzugriffe auf
Records (`12_bitfields`), die Qualitätszugriffe `.valid`/`.age`/`.or`
(sie brauchen den Aufbau des Prozessabbilds), Übergänge (`->`), `enter:`
und `exit:`, sowie die Zustandsverschachtelung tiefer als eine Ebene.

### 12.9 Uebergaenge: die Maschine ist eine Maschine

Mit den Uebergaengen ist aus der Schrittfunktion eine Zustandsmaschine
geworden. `corpus-try/01_minimal.takt` uebersetzt vollstaendig:
`IDLE --start--> WATCH --reset--> IDLE`, `SAFE --reset--> IDLE`, mit dem
`check` als Invariante von `WATCH`.

**Drei Regeln aus 5.2, die die erzeugte IR festhaelt:**

1. **Erst der `loop:`-Koerper, dann die Uebergaenge.** Ein `check` wirkt,
   bevor ein `when` den Zustand verlassen kann — umgekehrt waere der
   Zustand schon gewechselt, wenn die Invariante bricht, und die Meldung
   nennte den falschen. Ein Test vergleicht die Reihenfolge in der IR.
2. **Der erste passende Uebergang gewinnt**, in Quelltextreihenfolge. Das
   ist dieselbe Regel wie fuer Handler (8.7): Der Quelltext *ist* die
   Prioritaet, damit sie dasteht, statt hergeleitet werden zu muessen.
3. **`t_in_state` beginnt beim Eintritt von vorn.** Im erzeugten Code
   steht dafuer `store i64 -1`, weil das Ende des Schritts gleich um eins
   erhoeht — der erste Tick im neuen Zustand hat damit `t_in_state == 0`.
   Ohne das Zuruecksetzen misst `after d` die Zeit seit dem Start der
   Maschine statt seit dem Eintritt.

**Die Liste der vollstaendig uebersetzten Programme ist kuerzer geworden,
und das ist richtig so.** `03_sequences_and_faults` und `13_framing`
standen vorher darauf, weil ihre Uebergaenge gar nicht erst *versucht*
wurden. Jetzt melden sie, was ihnen fehlt (`after d` als Ausloeser,
Sequenzanweisungen) — eine ehrliche Liste ist kuerzer als eine, die
Unvollstaendiges mitzaehlt.

**Was als Naechstes fehlt**, in der Reihenfolge, in der die Korpusdateien
danach fragen: `after d` als Ausloeser (braucht `t_in_state` in
Nanosekunden und die Periode), Feldzugriffe auf Records, die
Qualitaetszugriffe `.valid`/`.age`/`.or` (sie brauchen den Aufbau des
Prozessabbilds), `enter:`/`exit:` und die Zustandsverschachtelung.

### 12.10 Daten, Qualitaet und Zeit

Der Codegen deckt jetzt 93 % der MIR-Knoten des Korpus. Vier Gruppen kamen
dazu, in der Reihenfolge ihrer Haeufigkeit.

**Varianten, Feldzugriffe, `as`.** Eine Enum-Variante ohne Felder *ist*
ihre Diskriminante — eine Konstante, die kein Register belegt. Sie steht
in der MIR (explizit oder fortlaufend vergeben); der Codegen rechnet sie
nicht nach, sonst gaebe es zwei Stellen, an denen sie entsteht. Ein
Feldzugriff wird `extractvalue`, nicht `getelementptr` plus `load`: Der
Record liegt als Wert vor, die Sprache hat keine Referenzen (11.2).

**Einheitenkonversion mit demselben Rechenweg wie der Interpreter.** Erst
der affine Versatz der Quelle, dann `* num`, dann `/ den`, dann der
Versatz des Ziels. Eine andere Reihenfolge waere mathematisch gleich, aber
in Fliesskomma eine andere Zahl — und Satz 9.4.4 verlangt dasselbe Bit.
Das ist der Grund, warum der Codegen hier nicht „vereinfachen" darf.

**Der Aufbau des Prozessabbilds ist eine ABI** (`src/image.rs`). 11.2 gibt
der Schrittfunktion den Zeiger, sagt aber nicht, wie das Abbild innen
aussieht; Runtime und erzeugter Code muessen sich einigen. Die
Entscheidung: ein Eintrag je Channel, `{ wert, qualitaet: i8, grund: i8,
alter: i64 }`.

Die Alternative waeren getrennte Arrays gewesen („struct of arrays").
Dagegen spricht 3.5: Fast jeder Zugriff liest *beide* — der implizite
Validitaets-Check steht vor jedem Lesen, und `x.or(d)` braucht Wert und
Qualitaet in derselben Zeile. Getrennte Arrays kosteten dafuer zwei
Cache-Zeilen statt einer.

Die Qualitaet ist ein `i8` und kein Bitfeld: Vier Werte passten in zwei
Bit, aber die Runtime schreibt sie aus einem anderen Crate, und ein
Bitfeld waere eine Verabredung, die keiner Seite ansieht. Ein Test haelt
die Skala mit `takt-hal` in Deckung — `.valid` heisst `<= SUSPECT`, und
das gilt nur, solange `Stale` und `Bad` darueber liegen.

**`after d` mit der Bedingung aus 7.1**, Zeichen fuer Zeichen wie im
Interpreter: `elapsed > 0 and elapsed >= d`. Der erste Vergleich ist nicht
ueberfluessig — 7.1 sagt „nie im Entry-Tick", und ohne ihn feuerte
`after 0 ms` schon beim Betreten, womit eine Sequenz in einem Tick durch
alle Schritte liefe.

Zwei neue Korpusdateien belegen die Gruppen, die bisher in keinem Programm
vorkamen: `15_quality.takt` (`.valid`, `.suspect`, `.stale`, `.or`) und
`16_timing.takt` (`after d`). Beide uebersetzen vollstaendig zu
Maschinencode.

**Stand:** 251 von 270 MIR-Knoten (93 %), 3 von 8 Maschinen vollstaendig.
Die Maschinenzahl waechst langsamer als die Knotenzahl, und das ist keine
Ungenauigkeit: Ein Programm ist erst uebersetzbar, wenn *alles* darin
uebersetzbar ist.

### 12.11 Geschachtelte Zustaende: aktiv ist ein Pfad

Mit der Verschachtelung uebersetzt der Codegen hierarchische Maschinen —
`17_nested.takt` mit zwei Ebenen geht vollstaendig durch clang.

**Der Kernsatz aus 5.2 lautet: Aktiv ist ein *Pfad*, nicht ein Zustand.**
Daraus folgt alles Weitere:

- Die `loop:`-Bloecke laufen von der Maschine abwaerts bis zum Blatt. Ein
  `check` auf einer Zwischenebene ist die Invariante *aller* Zustaende
  darunter — und genau dort stehen die Sicherheitsgrenzen einer Anlage.
- Die Uebergaenge werden vom Blatt aufwaerts geprueft: Der innerste
  Zustand entscheidet zuerst.
- Ein Uebergang auf einen zusammengesetzten Zustand betritt dessen
  `initial`-Kind, rekursiv bis zu einem Blatt (`initial_leaf`).
- `exit:` laeuft vom verlassenen Blatt aufwaerts bis unter den gemeinsamen
  Vorfahren, `enter:` von dort abwaerts. Ein Uebergang zwischen
  Geschwistern raeumt ihren Elternteil *nicht* ab.

**Zwei Befunde, beide von der Verschachtelung ans Licht gebracht.**

Der erste ist FB-73 und dieselbe Sorte wie der `enter:`-Fehler: Der
Codegen fuehrte nur den `loop:` des Blatts aus und liess die
Zwischenebenen fallen. Das Programm uebersetzte und verlor eine
Invariante.

Der zweite ist FB-72 und hat nur LLVM gefunden. Die Marke einer
Verzweigung wurde aus Blattnummer und Position in der Uebergangsliste
gebaut — bei zwei Ebenen liefert das zweimal denselben Namen, und aus dem
Sprungziel wird ein Block mitten im Block. Betroffen waere *jede*
hierarchische Maschine gewesen. Die Marke kommt jetzt aus einem Zaehler je
erzeugter Verzweigung, und ein Test prueft, dass keine zweimal vorkommt.

**Eine Praezisierung bei der Reproduzierbarkeit.** Der Test „dieselbe IR,
dieselbe Objektdatei" schlug fehl, und zwar zu Recht: Das COFF-Format
traegt an Byte 4 bis 7 einen Zeitstempel, den *clang* setzt. 11.3 verlangt
vom Compiler „keine Zeitstempel oder Pfade im Binary"; den Stempel des
Assemblers schaltet erst der Linkschritt ab. Der Test vergleicht jetzt den
Inhalt ohne den Kopf und nennt den Grund — dazu ein zweiter, der die
eigentliche Zusage prueft: Die erzeugte IR ist ueber Laeufe hinweg
bytegleich.

**Stand:** 281 von 300 MIR-Knoten (94 %), 5 von 10 Maschinen vollstaendig.

### 12.12 Beobachtungen, `abort` und die Runtime-ABI

**Alle zehn Korpusprogramme, die zu MIR uebersetzen, assemblieren jetzt zu
Maschinencode** — auch die drei, deren Maschinen der Codegen nur teilweise
senkt. Was er *erzeugt*, ist gueltig; eine halbe Funktion, die
assembliert, waere schlimmer als eine, die es nicht tut.

**Die zweite ABI steht** (`src/abi.rs`). `alert`, `log`, `measure`,
`verify` und `abort` kann der erzeugte Code nicht selbst: Ein
Trace-Eintrag braucht einen Puffer, ein `abort` die anderen Maschinen
(5.4). Er ruft die Runtime, und diese Aufrufe sind eine Verabredung wie
das Prozessabbild.

Die Signaturen tragen **Nummern statt Zeichenketten**. Der Text eines
`alert` steht in einer Tabelle, die der Compiler erzeugt, und der Aufruf
nennt ihren Index. Das spart auf einer MCU den Text im RAM (12.3), macht
die Signatur unabhaengig von der Zeichenkettendarstellung, und der Trace
bleibt kanonisch, weil die Runtime denselben Text nimmt wie der Report.

Die Flanke eines `alert` bildet die **Runtime**, nicht der erzeugte Code:
Sie kennt den vorigen Wert, der Code muesste ihn sonst im Zustand fuehren.

**Zuweisung an Felder und Elemente ueber `getelementptr`**, nicht ueber
`insertvalue` in einen geladenen Wert: Eine Zuweisung an `s.f` soll *das
Feld* schreiben, nicht den ganzen Record neu bauen. Bei einem `bytes<256>`
ist das der Unterschied zwischen einem Byte und 256.

**`after` mit einem Parameter als Frist.** Literal oder Parameter — beide
stehen fuer den Lauf fest, und nur dann kann die Schedulability (7.2) sie
beschraenken. Ein Ausdruck ueber Variablen haette einen Wert je Tick, und
`after` waere keine Frist mehr, sondern eine Bedingung.

**Zwei Befunde, beide von clang.**

FB-74: Nach einem `abort` schrieb der Codegen weiter. LLVM numeriert fuer
den unerreichbaren Rest einen impliziten Block, und die folgenden Register
hiessen anders als erwartet. `emit` verwirft jetzt, was hinter einem
Terminator steht — sonst muesste der Erzeuger an jeder Stelle wissen, ob
ein Zweig schon geendet hat, und diese Buchfuehrung zoege sich durch den
ganzen Codegen.

FB-75: `.bits` meldete den Typ des Zielausdrucks statt den seiner eigenen
Instruktion. Ein Knoten muss melden, was er *erzeugt* — die Verengung
macht der Cast, den die MIR darum herum setzt.

**Stand:** 293 von 301 MIR-Knoten (97 %), 7 von 10 Maschinen vollstaendig,
10 von 10 Dateien assemblieren.

### 12.13 Sammlungen, reine Funktionen, Schleifen

**98 % der MIR-Knoten**, und der Codegen hat jetzt alles, was ein
Protokollbaustein braucht.

**Sammlungen (3.9): `push`, `append`, `clear`.** Beschraenkt und
alles-oder-nichts — `append` haengt entweder die ganze Folge an oder
nichts. Ein Teilanhang liesse einen halben Rahmen im Puffer, den niemand
als Fehler erkennt. Der Rueckgabewert sagt, was geschah; ein voller Puffer
ist ein vorhersehbarer Betriebszustand und kein Fault.

`append` kopiert mit `llvm.memcpy`, nicht mit einer Schleife: Eine
Schleife braeuchte eine Schranke, die 4.1 ohnehin verlangt, und LLVM
kennt den Kopiervorgang besser.

**Reine Funktionen (4.4) als LLVM-Funktionen.** Eine `fn` ist rein — sie
liest ihre Parameter und liefert einen Wert. Damit darf LLVM sie
einbetten und ueber Aufrufe hinweg optimieren. Ihre Locals sind `alloca`
im Eintrittsblock; 12.3 verlangt einen statisch bekannten Stack, und das
ist erfuellt: keine Rekursion (13.4), keine dynamische Allokation, eine
`alloca` je Local.

**Eine Funktion, deren Rumpf der Codegen nicht senkt, wird deklariert
statt weggelassen.** Ein Aufruf auf ein undefiniertes Symbol ist gueltige
IR, und der Linker nennt es beim Namen; ein fehlendes Symbol laesst clang
die ganze Datei zurueckweisen, und der Fehler traefe dann alle Funktionen
statt der einen.

**`for i in range(n)` mit `break`** (4.1), **die Integer-Primitiven**
(`wrapping_*` als expliziter Umlauf, `saturating_*`, `rotl`/`rotr`,
`min`/`max`, `abs` — die letzten vier als LLVM-Intrinsics, weil sie von
Hand je drei Instruktionen und drei Gelegenheiten fuer einen Fehler
waeren), **`default` als `zeroinitializer`** (12.3: Nullen kosten kein
Flash), **Index** und **`.len`**.

**Die Intrinsic-Deklarationen entstehen bei Bedarf.** Sie sind je Breite
eigene Symbole — `@llvm.smin.i8` ist nicht `@llvm.smin.i32` —, also laesst
sich die Liste nicht vorab schreiben. `Module` sammelt, was gerufen wurde,
und schreibt die Deklarationen beim Abschluss.

**Der dritte stille Verlust, wieder von der Messung gefunden.** Der
Tickschritt erzeugte `on`-Handler nie; `12_bitfields` uebersetzte
vollstaendig und verlor seinen CAN-Handler (FB-76). Dreimal dieselbe
Ursache — nach FB-71 (`enter:`) und FB-73 (`loop:` der Zwischenebenen) —
spricht dafuer, dass die unabhaengige Zaehlung kein Beiwerk ist: Der
Codegen selbst kann ein stilles Weglassen nicht melden, weil er von dem
Knoten nichts weiss. Eine Maschine mit Handlern wird jetzt abgelehnt, bis
die Stroeme da sind.

**Stand:** 294 von 301 MIR-Knoten (98 %), 7 von 10 Maschinen vollstaendig,
10 von 10 Dateien assemblieren.

### 12.14 Bloecke, Stroeme und die Wrapper-Typen

**9 von 11 Maschinen uebersetzen vollstaendig, alle 11 assemblieren.**

**Blockinstanzen (5.7).** Ein Block ist ein Stueck Zustand mit Methoden;
seine Instanz lebt im Zustands-Struct der Maschine und ueberdauert den
Tick. Die Reihenfolge im Struct ist die des Interpreters
(`call_block_method`): erst die Variablen der Instanz, dann die Parameter
der Methode. Wer sie vertauscht, liest den Zustand als Argument — beides
uebersetzt, und nur die Zahlen sind falsch.

Das `stepped`-Flag ist 5.7 („step hoechstens einmal je Aktivierung").
Die Pruefung ist statisch, aber das Flag ist die Absicherung: Ein Filter,
der in zwei Zweigen desselben Ticks laeuft, haette einen Tick
uebersprungen, ohne dass es jemand saehe.

**`FnCtx` ist generisch ueber seine Variablenquelle geworden.** Eine
reine Funktion und eine Blockmethode unterscheiden sich nur darin, *wo*
ihre Variablen liegen; der Rumpf ist derselbe.

**Die dritte ABI: Stroeme** (`src/stream.rs`). Die Puffer gehoeren der
Runtime, nicht dem erzeugten Code — sie sind zwischen Treiber- und
Tick-Thread geteilt (12.2), und ihre Darstellung unterscheidet sich je
Profil (ein Byte-Ring auf der MCU, etwas anderes auf der Box). Der
Tickschritt sieht davon nur drei Aufrufe: `count`, `at`, `examined`.

Der Cursor bleibt im Zustand der Maschine (`cur[s, m]`, 9.6): Er gehoert
zum Konsumenten, nicht zum Strom, und zwei Maschinen lesen denselben
Strom unabhaengig voneinander.

**Der Handler-Durchlauf folgt dem Interpreter** (`dispatch`): Stroeme in
Deklarationsreihenfolge, je Strom das Fenster in `seq`-Reihenfolge, und
*jedes* Element gilt als untersucht — auch eines, auf das kein Handler
passt (9.6). Sonst saehe die Maschine es im naechsten Tick wieder.

Eine Schleife und keine abgerollte Folge: Die Fenstergroesse steht erst
zur Laufzeit fest, nur ihre Schranke ist bekannt. 4.1 verlangt eine
Schranke, nicht eine feste Zahl — und `CAP` Durchlaeufe abzurollen waere
bei einem Ring von 256 Elementen unbrauchbar.

**Die Wrapper-Typen (3.8).** `T?` ist `{ wert, gueltig }`, `T!E` ist
`{ wert, fehler, ok }`. Das Flag steht am Ende, damit der Wert an
derselben Stelle liegt wie ohne Wrapper — das Auspacken ist dieselbe
Instruktion fuer beide Formen.

**Ein Befund im Sema (FB-77):** `block_type` baut den Namen `block:{id}`
und wirft ihn weg, ohne ihn zu registrieren — jede Blockinstanz bekommt
denselben Platzhaltertyp. Fuer den Interpreter faellt das nicht auf, weil
er die `BlockId` im Wert fuehrt; der Codegen braucht den Typ, um den
Struct zu bauen, und liest die Identitaet darum aus
`Layout::block_instances`. Die Registrierung gehoert in M6, wenn der
Handle-Raum ohnehin zu ordnen ist.

**Was bleibt**, und es ist eine kurze Liste: `decode` (8.6, das
Drahtformat), `match` ueber Summentypen, Teilbereiche (`x[a..b]`),
`interp` ueber `table<A, B>` — dessen Typ traegt keine Laenge, was
zugleich eine Luecke fuer `takt size` (11.5) ist — und Handler mit
Muster.

**Stand:** 338 von 345 MIR-Knoten (98 %), 9 von 11 Maschinen
vollstaendig, 11 von 11 Dateien assemblieren.

### 12.15 Das Drahtformat, `match` und die Wrapper

**368 von 373 MIR-Knoten (99 %), 10 von 11 Maschinen, 11 von 11 Dateien
assemblieren.** Was bleibt, ist `table<A, B>`.

**`decode`/`encode` (3.7).** Der Plan steht im Typ — `FieldDef::offset`,
`RecordDef::wire_size`, `WireLayout::endian` hat das Sema gerechnet und
geprueft (Pruefung 46). Der Codegen setzt ihn um und rechnet ihn nicht
nach: Zwei Stellen, an denen ein Offset entsteht, waeren eine zu viel.

Darum kein Schleifencode. Jedes Feld hat seinen festen Platz; der Codegen
rollt sie ab. Das ist auch, was 11.5 fuer den Stack annimmt: keine
Laufvariable, kein Index, keine Schranke, die jemand pruefen muesste.

`decode` faultet nie (3.7). Zu kurzer Puffer, verletztes Konstantenfeld
oder ein Wert ausserhalb der Range ergeben `none` — ein fremder Rahmen
auf dem Bus ist ein Betriebszustand, kein Programmfehler.

Die Byte-Reihenfolge kommt aus `llvm.bswap`, nicht aus einer Folge von
Schiebeoperationen je Breite.

**`match` (6.1)** als Kette von Vergleichen, kein `switch`: Die Muster
koennen Bereiche sein, und LLVM macht aus einer Kette gleicher Vergleiche
selbst einen `switch`, wo es sich lohnt. Die Reihenfolge ist die des
Quelltexts — ein Umsortieren waere eine andere Semantik, sobald sich zwei
Muster ueberschneiden.

Bei `T!E` steht die Fehlerdiskriminante im Feld 1, nicht im Feld 0; dort
steht der Wert (3.8).

**Zwei Befunde, beide von der Pruefung.**

FB-78: `Checked{Missing}` ist das *Auspacken* eines Wrappers, nicht nur
eine Pruefung. Der Codegen reichte den Knoten durch, und jeder folgende
Zugriff ging auf den Wrapper statt auf den Inhalt. Im Interpreter faellt
das nicht auf, weil `Value::Optional` sich beim Lesen selbst auspackt.
Gefunden hat es clang.

FB-79: Der Markenzaehler einer Funktion begann je Funktion bei null, die
Marken stehen aber im Modul — zwei Funktionen mit je einem `if` hatten
beide `dann1`. Derselbe Befund wie FB-72, eine Ebene hoeher, und wieder
von einem Test gefunden, der nach jedem neuen Erzeuger laufen sollte.

**Was `table<A, B>` blockiert**, ist keine Codegen-Frage: Der MIR-Typ
traegt keine Laenge. Die Stuetzstellen stehen nur im Wert, und ohne
Schranke am Typ kann weder der Codegen einen Struct bauen noch `takt
size` (11.5) rechnen. Beides gehoert zusammen geloest.

### 12.16 Der Codegen ist vollstaendig

**368 von 368 MIR-Knoten (100 %), 11 von 11 Maschinen, 11 von 11 Dateien
assemblieren.** Jedes Korpusprogramm, das fehlerfrei zu MIR uebersetzt,
uebersetzt auch zu x86-64-Maschinencode.

**`interp` (3.9) war der letzte Knoten — und die Tabellenluecke gab es
nicht.** Ich hatte `table<A, B>` als Luecke im Typsystem gemeldet, weil
der MIR-Typ keine Laenge traegt. Die Pruefung ergab das Gegenteil: Die
Grammatik nennt ihn ohne Laenge (2.3), 3.9 verlangt eine Konstante, und
der Compiler faltet sie an die Aufrufstelle. Im MIR steht dort ein
Array-Literal mit bekannter Laenge.

Ein Laengenparameter am Typ waere Redundanz gewesen, und die Schranke,
die ich zwischenzeitlich geraten hatte (`TABLE_MAX = 64`), haette
Speicher belegt, den `takt size` (11.5) falsch ausgewiesen haette. Der
Entwurf von 3.9 ist an dieser Stelle besser als mein erster Reflex
(FB-80).

Die Folge fuer den Codegen: Die Suche ist abgerollt, und die abgerollte
Form *ist* die Schranke, die 4.1 verlangt. Die Rechnung ist die des
Interpreters, Operation fuer Operation — `y0 + (y1 - y0) * (x - x0) /
(x1 - x0)`, jede einzeln gerundet. Eine andere Klammerung waere
mathematisch gleich und in Fliesskomma eine andere Zahl (Satz 9.4.4).

An den Raendern wird geklemmt, nicht extrapoliert: Das haelt die Funktion
total (4.1) und vermeidet, dass ein Wert ausserhalb der Kennlinie eine
Zahl erfindet.

**Die beiden Listen im Test sind jetzt dieselbe.** `VOLLSTAENDIG` ist
`UEBERSETZBAR` — und dass sie es ist, hat den Codegen fertig gemacht.

**Damit ist Schritt 7 abgeschlossen.** Was folgt, ist Schritt 8: das
differentielle Testen, die Abnahme von M4. Der Codegen liefert dafuer,
was er liefern muss — ein Programm, das laeuft; der Interpreter ist das
Orakel, gegen das es laufen muss.

### 12.17 Schritt 8: die Abnahme

**Zwoelf Programme laufen zweimal — im Interpreter und als uebersetztes
Binaerprogramm — und liefern dieselben Outputs.** Dazu ein Fuzzer, der
Programme erzeugt statt sie zu lesen. Satz 9.4.4 hat damit seinen Test.

**Der Testrahmen ist C.** Der erzeugte Code spricht die C-ABI, und clang
uebersetzt beides in einem Zug; ein Rust-Gegenstueck braeuchte `unsafe`
fuer jeden Zeiger. Die Versaetze kommen aus `takt-llvm`, nicht aus einer
zweiten Rechnung — ein Rahmen, der die Adressen selbst ausrechnet, prueft
seine eigene Rechnung gegen die des Codegens und findet genau dann
nichts, wenn beide denselben Fehler machen.

**Verglichen werden Outputs, nicht Zustaende** (9.4.4 spricht von
Outputs; 2.5 hat daraus die Abnahme gemacht). Fliesskomma wird bitgenau
verglichen, nicht mit Toleranz: Eine Toleranz waere genau die Abweichung,
die zu finden der Test da ist.

**Sieben Fehler, die kein anderer Test finden konnte.** In allen sieben
Faellen ist die IR gueltig und assembliert — sie *laeuft* nur anders als
die Spezifikation:

| | Befund | wer fand ihn |
|---|---|---|
| FB-81 | Latch, Parameter und Commands wurden indiziert statt aufsummiert | der Entwurf des Rahmens |
| FB-82 | die Entry-Tick-Regel (5.2 Regel 4) fehlte | der Korpus |
| FB-83 | der Fault-Pfad merkte nur vor, statt das Ziel zu betreten | der Korpus |
| FB-84 | `Checked{Valid}` wurde durchgereicht statt geprueft | der Korpus |
| FB-85 | die Anfangswerte der Maschinenvariablen fehlten | der **Fuzzer** |
| FB-86 | die Laufzeitpruefungen aus 4.1 fehlten vollstaendig | ein Programm von Hand |
| FB-87 | eine reine Funktion konnte nicht faulten | der Einbau von FB-86 |

**Der Fuzzer hat sich sofort bezahlt gemacht** (FB-85). Der Korpus
konnte den Fehler nicht finden: Seine Programme lesen ihre Variablen
meist erst, nachdem sie sie geschrieben haben, und eine Null am Anfang
faellt dort nicht auf. Der Fuzzer erzeugt Ausdruecke, die *nur* aus der
Variablen und Konstanten bestehen — dort ist der Unterschied das
Ergebnis. Genau dafuer ist er da.

**Und er hat eine Grenze**, die dasteht, damit niemand die Zahl fuer mehr
haelt, als sie ist: Er meidet Faults per Konstruktion, weil ein
gefaultetes Programm nichts mehr zu vergleichen hat. FB-86 — die
fehlenden Laufzeitpruefungen, der schwerste der sieben — brauchte ein von
Hand geschriebenes Programm. `corpus-try/19_faults.takt` haelt den Fall
jetzt fest.

**Was der Rahmen nicht prueft**, und warum das hier steht: Er hat keine
Treiber (also keine Eingaben ausser der Anfangsqualitaet), keine Uhr und
keine Abort-Phase. Ein Lauf prueft damit den Anfangszustand und seine
Fortschreibung. Das ist weniger, als 13.8 am Ende verlangt — die
Eingaben kommen mit Schritt 10 (Record/Replay), der sie ohnehin braucht.

### 12.18 Schritt 9: die kuratierte Menge

**`takt-native` liefert vier Pruefsummen** — `crc32`, `crc32c`, `crc16`,
`sum8` —, und die Deklaration einer Funktion ausserhalb der Menge ist ein
Compile-Fehler. Damit ist 4.5 („v1: nur die kuratierte, mitgelieferte
Menge") durchgesetzt statt nur beschrieben.

**Die Aufnahme ist dieselbe Huerde wie bei `libtaktm`** (13.8): 40
Vektoren in `grammar/takt-native.md`, drei Tests, die Spezifikation und
Menge im Gleichschritt halten — darunter der umgekehrte, der keinen
Vektor fuer eine Funktion zulaesst, die es nicht gibt.

Die Vektoren kommen **aus den Normen, nicht aus der Implementierung**:
`crc32` aus `zlib.crc32`, die uebrigen nach ihren Katalogen nachgerechnet.
Waeren sie aus dem eigenen Code erzeugt, pruefte der Test ihn gegen sich
selbst. Die Kataloge-Pruefwerte (`CBF43926`, `E3069283`, `BB3D` fuer
`123456789`) stehen zusaetzlich als eigener Test, weil dort ein
vertauschtes Polynom sofort auffaellt.

**Warum Pruefsummen zuerst.** Sie sind exakt spezifiziert, kommen in jedem
Protokoll vor, und ihre Kosten sind linear in der Laenge — also als
Vertrag angebbar, ohne etwas zu messen. `cost` nennt darum die Kosten *je
Byte*; die Laenge steht im Typ.

**Bitweise gerechnet, nicht ueber eine Tabelle.** Eine Tabelle waere
schneller und kostete 1 KiB Flash je Polynom (12.3). Vor allem aber ist
die bitweise Form die, die *offensichtlich* der Norm entspricht — sie ist
die Definition, eine Tabelle ihre Optimierung. Fuer die TCB (9.5) zaehlt
das mehr als der Takt.

**Das Crate hat keine Abhaengigkeit.** Eine native Funktion gehoert zur
TCB (9.5), und jede Abhaengigkeit gehoerte dann dazu.

**Der Codegen ruft sie als externes Symbol** (`takt_native_crc32`), und
der Testrahmen liefert dieselbe Rechnung in C — damit prueft die Abnahme
sie mit, statt sie zu umgehen. `corpus-try/20_native.takt` uebersetzt in
beiden Implementierungen und liefert `2286445522` fuer `crc32("123")`,
was `zlib` bestaetigt.

**Ein Befund am Rande** (FB-88): Der Rahmen las vorzeichenlose Werte als
vorzeichenbehaftet, weil LLVM das Vorzeichen nicht im Typ fuehrt. Harmlos,
weil er nur die Ausgabe betraf — aber er haette jeden Vergleich
vorzeichenloser Werte scheitern lassen, und die Ursache waere beim Codegen
gesucht worden.

### 12.19 Schritt 10: Aufzeichnung und Wiedergabe

**Eine Aufzeichnung ist ein Trace mit Kopf.** Das Format aus
`grammar/trace.md` ist zeilenorientiert, kanonisch und hat Vektoren; ein
zweites braechte einen zweiten Leser (6). Der Kopf traegt, was 12.5
verlangt und eine Trace-Zeile nicht sagen kann: welches Programm lief,
mit welchen Parametern, welcher Toolchain — und welche nativen Funktionen
es benutzt, weil 4.5 das verlangt, „damit die erweiterte TCB sichtbar
bleibt".

**Aufgezeichnet werden die Eingaben, nicht die Ausgaben.** Das ist Satz
9.4.1: Der Trace ist eine Funktion von (s0, I_k), also genuegt I_k. Eine
Datei, die schon enthaelt, was sie belegen soll, belegt nichts.

**Der Logik-Hash im Kopf ist die Bedingung des Satzes aus 12.5.**
„Abweichung = Fehler in Runtime oder Treiber, nie in der Logik" gilt nur,
wenn die Logik dieselbe ist. `takt replay` lehnt eine Aufzeichnung zu
einem anderen Programm darum ab, statt Abweichungen zu melden, die nichts
bedeuten.

**Kein Zeitstempel, kein Pfad im Kopf** (11.3): Eine Aufzeichnung, die
sich bei jedem Lauf unterscheidet, waere schlecht zu vergleichen; *wo*
etwas lief, gehoert nicht zur Semantik. Die Zahl der Ticks steht dagegen
schon im Kopf — ein Lauf kann laenger sein als seine letzte Eingabe, und
meistens ist er es.

**Die groesste Luecke der Abnahme ist damit kleiner geworden.** Der
Testrahmen bekommt jetzt Eingaben: Was der Interpreter als Stimulus
sieht, schreibt er je Tick in das Prozessabbild. Commands zuerst, weil
sie die einfachste Form sind (ein Puls, ein Byte, 8.5) und weil der
Korpus sie benutzt; skalare Lieferungen brauchen den Treiber und kommen
mit Schritt 5.

**Und sie hat sofort einen Fehler gefunden** (FB-89): `t_in_state` begann
bei -1 statt bei null, weil ich um die Reihenfolge herum gerechnet hatte
statt um die Bedeutung. Der Entry-Modus liest den Zaehler noch im selben
Tick, also feuerte jede `after`-Frist einen Tick zu spaet. Bei 200 ms und
10 ms Tick sind das 5 Prozent — der Sorte nach ein Fehler um eins, in der
Wirkung keiner.

### 12.20 Schritt 5: `linux_rt`

**Die Entscheidung aus FB-65 war auf einer unvollstaendigen Pruefung
gebaut.** Ich hatte gemeldet, 12.2 brauche `libc` oder `unsafe`, ohne
nachgesehen zu haben, ob ein sicheres Bindungs-Crate existiert. Es
existiert: `rustix` 1.1.4 liefert `clock_nanosleep_absolute`, `mlockall`
und `sched_setaffinity` als sichere Funktionen mit `io::Result`.

**Damit bleibt `unsafe_code = "forbid"` im ganzen Workspace**, und das
ist nach 13.4 kein Schoenheitsargument: „kein unsafe, nirgends" ist eine
Zusage in einem Satz, „kein unsafe ausser hier, begruendet wie folgt" ein
Argument je Audit.

Die Abhaengigkeit erweitert die TCB nicht. 9.5 nennt Runtime, OS und
Hardware bereits darin; `rustix` ist eine duenne Schicht ueber Syscalls,
die ohnehin dazugehoeren. Sie macht den Aufruf sicher, nicht die Zusage
groesser.

**`SCHED_FIFO` setzt die Runtime bewusst nicht** (FB-90). Dieselbe Zeile
in 12.2 nennt `isolcpus` und `nohz_full` — Kernel-Bootparameter, die kein
Programm setzt. Die Prioritaet steht in derselben Aufzaehlung und ist
derselben Art: Betriebskonfiguration. Ein Programm, das sie sich selbst
gibt, braucht CAP_SYS_NICE oder root und muesste privilegiert
ausgeliefert werden — schlechter fuer Sicherheit und Betrieb als
`chrt -f 80 ./programm`.

**Stattdessen misst die Runtime, was sie bekommen hat, und sagt es.**
`Guarantee` traegt die drei Teile aus 12.2 — Schedulingklasse, `mlockall`,
Kernbindung — und geht als Zeilen in den Lauf-Header (11.3). Der
Unterschied zwischen „lief unter `linux_rt`" und „lief unter `linux_rt`
*mit* der Zusage" steht damit in der Aufzeichnung, nicht in der
Erinnerung des Bedieners.

Das ist der Kern: **Eine Zeitgarantie, die still ausfaellt, ist schlimmer
als eine, die fehlt** — eine Messung unter ihr sieht gueltig aus. Fehlt
ein Teil, nennt die Meldung den Weg (`chrt`, `taskset`,
RLIMIT_MEMLOCK); ein Befund ohne Abhilfe ist eine Klage.

**Die Uhr benutzt absolute Fristen** (`TIMER_ABSTIME`) auf
`CLOCK_MONOTONIC`: Ein zu spaeter Tick verschiebt die folgenden nicht,
und weder Zeitumstellung noch NTP springen hinein. 7.4 haelt die
Systemzeit ausdruecklich aus der Semantik heraus; dieselbe Ueberlegung
gilt fuer die Tickquelle.

**Ausserhalb von Linux baut und laeuft das Crate trotzdem** — mit grobem
Schlaf und feinem Warten. Der Rueckfall traegt die Zusage *nicht*, und
`Guarantee` meldet dort `Unknown`. Er ist da, damit sich die Runtime auf
jedem Rechner testen laesst, nicht damit sie dort Echtzeit behauptet.

**Geprueft auf beiden Zielen.** Der Workspace uebersetzt fuer
`x86_64-unknown-linux-gnu` ohne Fehler und ohne Warnung; die Tests laufen
auf Windows. Der erste echte Lauf unter PREEMPT_RT steht aus — dort
misst sich, was hier nur vorbereitet ist.

### 12.21 Schritt 11: x86-64 ≡ aarch64

**Fuenf Korpusprogramme laufen auf beiden Architekturen und liefern
Zeichen fuer Zeichen dasselbe.** Uebersetzt mit clang fuer beide Triples,
ausgefuehrt auf x86-64 nativ und auf aarch64 unter qemu.

**Der Codegen brauchte dafuer keine Aenderung**, und das ist der Befund:
Das Triple ist ein Parameter, kein Sonderfall. x86-64 und aarch64
gehoeren in 12.8 derselben Zielklasse an („64-Bit Linux", 64 Register,
f32 und f64 mit FMA), und die Unterschiede liegen unterhalb der IR —
Registerbelegung, Befehlsauswahl, Aufrufkonvention. Das ist LLVMs Arbeit.

Ein Test haelt das fest: **Die IR unterscheidet sich nur im Triple.** Das
ist die Bedingung, unter der Satz 9.4.4 eine Aussage ueber *eine*
Uebersetzung ist und nicht ueber zwei Programme.

**Warum das mehr ist als ein weiterer Lauf.** Die beiden Architekturen
unterscheiden sich in ihren Fliesskommabefehlen. Ein `fma`, das die eine
hat und die andere emuliert, waere eine andere Zahl. Dass beide dieselbe
liefern, ist die Zusage, auf der Simulation und Zertifizierung aufbauen.

### 12.22 Der erste Lauf unter Linux

Mit Docker laufen die Linux-Pfade jetzt wirklich, nicht nur im
Cross-Check. Drei Befunde:

**Die Zeitgarantie wird korrekt erkannt.** Ohne `chrt` meldet `Guarantee`
`scheduling other`, `cpus 6`, `echtzeit nein` und nennt die Abhilfe; mit
`chrt -f 80 taskset -c 0` meldet sie `fifo`, `cpus 1`, `echtzeit ja`. Das
ist der Entwurf aus Schritt 5, an der Wirklichkeit geprueft.

**Die Zusage wirkt messbar.** 1000 Ticks zu je 1 ms:

| | Median | p99 | Maximum |
|---|---|---|---|
| ohne Echtzeit | 69 us | 133 us | 220 us |
| mit `SCHED_FIFO` auf einem Kern | 20 us | 89 us | 145 us |

**Die Zahlen sind Hausnummern, keine Abnahme** (FB-91). Ein Container
teilt sich den Kern mit dem Wirt, und seine Uhr ist die virtualisierte.
Der Faktor 3,4 zeigt, *dass* die Zusage wirkt; die absoluten Werte gehoeren
auf die Zielhardware (`takt bench`, 13.8, M5). `tools/linux.sh` nennt den
Vorbehalt, und das Beispiel `jitter` druckt den Befund neben die Zahlen —
eine Messung ohne ihren Kontext ist eine Zahl ohne Bedeutung.

**Der Weg dorthin steht als Werkzeug da**, nicht als Merkzettel:
`tools/Dockerfile.linux` baut die Umgebung (clang, aarch64-Kette, qemu),
`tools/linux.sh` fuehrt einen Befehl darin aus. `--target-dir` zeigt in
ein benanntes Volume, weil `cargo` Tausende kleiner Dateien schreibt und
jede ueber 9p einen Roundtrip kostet.


## 12.23 Was fremder Code fand

Die drei Programme in `feedback/` sind gegen ältere Referenzstände
geschrieben und benutzen die Sprache anders als der eigene Korpus: mehr
Protokollarithmetik, längere Signaturen, Namen, die ein Ingenieur
selbstverständlich wählt. Sie gegen die Grammatik zu ziehen brachte
sechs Stellen ans Licht, an denen der Compiler von der Referenz abwich —
und keine davon hätte der eigene Korpus gefunden, weil er dieselben
Annahmen trägt wie der Compiler.

**Zwei widersprachen der Referenz sichtbar.** Eine `pub var` war nur
sichtbar, wenn ihre Maschine vorher deklariert wurde (FB-99), obwohl
Grundentscheidung 6 die Reihenfolge „beweisbar irrelevant" nennt; zwei
Maschinen mit Gegenkopplung sind aber nicht beide zuerst deklarierbar,
und das ist der Normalfall. Und das Element eines Skalarstroms war gar
nicht erreichbar (FB-98) — die Bindung trug nur `.t` und `.seq`, womit
ein Byte-Strom aus einem Handler heraus nicht verarbeitbar war.

**Die Behebung von FB-99 war die heikelste Stelle des Durchgangs.** Die
Vorschau muss die Variablenliste *deckungsgleich* anlegen, nicht nur die
öffentlichen: `VarId` ist ihr Index, und eine Lücke verschiebt jede
aufgelöste Referenz um eins. Der erste Versuch tat genau das — und die
Übersetzung lief fehlerfrei durch, nur las das Programm die falsche
Variable. Ein Fehler, den kein Typsystem meldet und den nur ein Lauf
zeigt: Der Test prüft darum den gelesenen *Wert*, nicht die Auflösung.

**Drei waren Implementierungsfehler mit lokaler Ursache.** Eigene
Blockmethoden waren nicht aufrufbar (FB-96), weil die Erkennung an einer
festen Namensliste hing statt am Empfänger — `stopwatch` aus 11.4 ließ
sich nicht übersetzen. `pulse` scheiterte in einer `sequence` (FB-97),
weil deren Senkung an der Auflösung vorbeilief. Und `-32768` passte
nicht in `i16` (FB-95), weil das unäre Minus ein eigener Knoten ist und
der Betrag für sich geprüft wurde; betroffen war genau die Untergrenze
jeder signierten Breite.

**Zwei Befunde blieben, was sie waren, und bekamen eine bessere
Meldung.** Ein Enum trägt seine Diskriminante ins Drahtformat, nicht in
die Rechnung (FB-100) — die Rückrichtung wäre partiell, und `match`
trägt die Erschöpfung; die Diagnose nennt jetzt den Weg, statt nur
abzulehnen. Ebenso beim Umbruch vor `->` (FB-93): Die Sprache ist im
Recht, 2.1 regelt den Fall, aber die Meldung schlug `inout` vor, was
dort nicht passt.

**Offen bleibt eine Entwurfsfrage** (FB-101): ob eine Stream-Bindung als
Ganzes dort einsetzbar sein soll, wo ihr Element steht. Die Referenz
greift durchweg feldweise zu und übergibt nirgends eine Bindung; eine
Projektion wäre eine Ergänzung, keine Wiederherstellung — und damit
keine Fehlerbehebung. Sie gehört entschieden, nicht nebenbei eingeführt.

**433 Tests**, elf davon neu (`crates/takt-sema/tests/fremdcode.rs` und
einer in `parser_errors.rs`). Die Fehlerzahl der drei Programme fiel von
39/83/56 auf 35/82/52; was bleibt, sind Stufenmeldungen für `persist`,
`property`, `scenario`, `follows`, Einheiten auf Integern und `port` —
Konstrukte aus M6 und M8 — und ihre Folgefehler.


## 12.24 Die Bindung wird ein Wrapper

Aus FB-101 — ob eine Stream-Bindung als Ganzes dort stehen darf, wo ihr
Element steht — wurde beim Abwägen eine Sprachentscheidung. Der Grund war
nicht die Frage selbst, sondern was beim Nachmessen daneben lag.

**Ein Protokollfeld war unerreichbar.** Ein Rahmenformat mit eigenem Feld
`seq` — in Protokollen der Normalfall — wurde von der Stromnummer
verdeckt. Gesendet `seq = 99`, gelesen `1, 2, 3`: kein Fehler, keine
Warnung, nur der falsche Wert. Dasselbe drohte bei `t`, `data` und
`text`, die 2.5 als Record-Feldnamen ausdrücklich erlaubt
(`CanFrame.data`). Damit war die Frage nicht mehr, ob eine Projektion
bequem wäre, sondern ob zwei Namensräume vermischt bleiben dürfen.

**Die Referenz hatte den Fall schon entschieden**, nur an anderer Stelle:
Auf einem Wrapper meint `x.name` immer den Wrapper, und Felder des
Inhalts sind erst nach dem Auspacken erreichbar (2.5). Eine Bindung ist
genau das — ein Element mit Metadaten darüber. Sie trägt jetzt `.t`,
`.seq` und den Inhalt unter *einem* Namen (`.data`, bei `line<N>`
`.text`); Elementfelder stehen darunter (`f.data.id`).

Drei Wirkungen. Die Verdeckung verschwindet strukturell statt durch eine
Verbotsliste. Der Inhalt steht als ein Wert zur Verfügung, was
`parse_frame(f.data)` ohne Projektion erlaubt — die Form, die 4.1 und
13.8 verlangen. Und ein Zugriffsmuster gilt für alle Elementarten statt
vier verschiedener; ein `stream<u8>` hatte zuvor gar keinen Zugriff auf
seinen Wert.

**Die Migration war kleiner als erwartet.** Im Korpus fiel genau *eine*
Stelle an (`fr.head` → `fr.data.head`), in `feedback/` zwanzig, alle
mechanisch. Im DMX-Programm entfällt eine feldweise Kopie ersatzlos: Was
dort wegen der fehlenden Übergabe stand, ist jetzt `parse_rdm(f.data)`.
Der Interpreter wurde einfacher — der Zweig, der Record-Felder
durchreichte, ist weg.

Nachgezogen sind 8.6, 8.7 und 2.5 der Referenz, das Entscheidungslog
(Kapitel 16), `plan/m2.md` 1.7, der Kommentar an `on_handler` in der
EBNF und `plan/features.csv` (`PAR-8.7-Bindung_als_Wrapper`). Parser,
Formatter und AST blieben unberührt: Die Änderung betrifft die Semantik
der Bindung, nicht ihre Syntax — `as IDENT` parst unverändert.

**437 Tests**, vier davon neu, darunter der Verdeckungsfall als Lauf. Die
Golden-Datei des Syntaxbaums änderte sich an genau der migrierten Zeile.


## 12.25 Die Inventur nachgezogen

Vor dem Übergang nach M5 stand die Frage, was laut Inventur noch offen
ist. Die Antwort war zunächst irreführend: 209 offene Einträge, davon 89
unter M1 — einem Meilenstein, der seit Monaten abgeschlossen ist.

**Der größte Teil war Buchhaltungsrückstand.** Stichproben zeigten es
sofort: `SEM-3.10` (Bit-Operationen) steht in `call.rs`, `SEM-4.5`
(Natives) ist ein eigenes Crate, `SEM-7.5` (`at`/`pulse`) läuft im
Interpreter. Der Status war nur nie nachgezogen worden — was die Inventur
als Steuerungsinstrument entwertet, denn `plan.md` 6.4 macht sie
ausdrücklich zum Meilenstein-Gate.

**Der Abgleich lief belegbasiert**, wie dieselbe Regel es verlangt:
abgehakt wird, was ein laufender Test belegt. Für die Absatzregeln
entstand je ein Probeprogramm — übersetzt es, ist die Regel umgesetzt;
meldet es eine Stufe, gehört der Eintrag in einen späteren Meilenstein;
scheitert es anders, fehlt etwas. Für die Fault-Arten und Qualitäten
zählte, ob ein Test sie auslöst, nicht ob der Code sie kennt. Die
Beweisverpflichtungen T1–T16 sind keine Features, sondern Schritte im
Beweis von Satz 9.4; jeder wurde seinem Träger zugeordnet (T4 dem Lemma
9.3.1 in `theorems.rs`, T9 dem Lemma 9.6.1 in `streams.rs`, T15 den
Rand-Selbstprüfungen RAND-3/4).

**Was dabei ehrlich offen blieb, ist aufschlussreicher als die Zahl.**
`TimingFault`, `ScheduleOverflow` und `ArithmeticFault` sind
implementiert, aber kein Test löst sie aus — das ist `teilweise`, nicht
`fertig`. Die DFA-Tabellen in `takt size` weist das Werkzeug selbst als
`offen` aus: Die Rechnung steht, ihre Eingabe kommt mit 8.10 und 13.8.
Und T1 (Typ-Soundness) und T2 (Totalität der Primitive) bleiben offen,
weil sie Eigenschaften der Sprache sind und kein einzelner Test sie
belegt.

Elf Einträge waren im falschen Meilenstein: Matrizen, Jobs, `persist`,
`idle`, Wall-Clock und Systemschlaf melden heute eine Stufe und gehören
damit zu M6; `Node` ist verteilte Ausführung und damit v2; die
Timer-Präzision aus 7.5 braucht Hardware (M5).

**Ergebnis: 572 → 628 fertig, 209 → 143 offen.** Unter M1 bleiben vier
Einträge, keiner mit implementierbarem Inhalt.

**`takt test` nach M6 verschoben.** Es stand unter M4, obwohl die
Zuordnungstabelle in `plan.md` es seit dem 10.09. nach M5/M6 legt — die
CSV war inkonsistent. Sachlich entscheidet 13.6: Das Kommando führt
`scenario`-Deklarationen als Sim-Läufe aus, und Szenarien sind v1.1. Ohne
sie hätte es nichts auszuführen. Der M4-Exit ist davon unberührt; er
verlangt differentielles Testen, Echtzeitläufe und Replay, nicht das
Kommando.


## 12.27 Fault-Ziele im Codegen — und zwei Fehler dahinter

`Target` hat drei Varianten, der Codegen kannte eine. `Target::State`
war gebaut, `Faulted` (5.3) und `Fault(kind)` (6.2) brachen mit
`NotYet { what: "Uebergangsziel" }` ab — jedes Programm mit
`until … timeout` oder `-> FAULTED` fiel damit aus dem nativen Pfad.

**Die beiden gehen verschiedene Wege, und der Unterschied ist der Fault
selbst.** Ein Sequenz-Timeout *ist* ein Fault: Er wird in `pending`
vorgemerkt — mit seiner Art, denn nur der Übergang kennt sie — und nimmt
dann den Fault-Pfad des Blatts, denselben, den ein gescheiterter `check`
nimmt. `-> FAULTED` dagegen ist ein Ziel, kein Fault: Die Konfiguration
bekommt den Index hinter dem letzten Blatt, den der `switch` nicht
trifft, und es läuft kein Nutzercode mehr.

Der Fault-Pfad selbst war bereits vollständig da. Die Arbeit bestand
darin, die beiden Ziele mit ihm zu verdrahten — nicht darin, ihn zu
bauen.

### Was der Weg dorthin zutage förderte

Zwei Fehler, beide älter als diese Änderung und beide in der Wirkung
unangenehmer als die Lücke, die ich behob.

**`t_in_state` begann nativ einen Tick zu spät** (FB-106). Der
Interpreter schreibt den Zähler schon am Ende der Initialisierung fort —
9.4 sagt es wörtlich: „der Zähler wird wie am Ende jedes Ticks
fortgeschrieben". `init_function` tat das nicht, und damit feuerte jede
Frist einen Tick zu spät. Derselbe Fehler um eins wie FB-89, nur an der
anderen Seite; bei 30 ms Frist und 10 ms Tick sind das 33 Prozent.

**In `FAULTED` behielten die Outputs ihren letzten Wert** (FB-107). Der
Codegen ließ sie stehen, mit dem Kommentar, die Runtime stelle sie — der
Interpreter setzt sie selbst, im selben Tick. Die Wirkung ist genau die,
gegen die `safe` gedacht ist: Ein Ventil, das offen stand, bliebe offen.

**Warum beides nie aufgefallen war**, ist in beiden Fällen dasselbe: Der
eigene Korpus fuhr die Konstrukte nicht in einer Form, die den
Unterschied zeigt. Jedes Programm deklarierte ein Fault-Ziel und nahm
damit den anderen Zweig; jedes `after` stand in einem Trace, dessen
Verschiebung nicht auffiel. Der differentielle Test misst nur, was der
Korpus fährt — und der Korpus wächst nur, wenn jemand ihn erweitert.

**Zwei neue Korpusprogramme** fahren die Wege jetzt: `21_fault_targets`
(Sequenz-Timeout und `-> FAULTED` als Übergang) und `22_faulted_outputs`
(Fault ohne Ziel, dazu die Frist als solche). Beide stimmen bitgenau mit
dem Interpreter überein.

**438 Tests.** Damit bleibt unter M4 `takt graph` und die Aufnahme der
Beispiele 14.1–14.6 in den differentiellen Korpus (FB-102) — wofür
dieser Schritt die Voraussetzung war: Ohne die Fault-Ziele hätte 14.1
gar nicht übersetzt.


## 12.28 `takt graph` und die Beispiele auf beiden Wegen

### Die Antwort auf die Plant-Modelle

Fünf der sechs Referenzbeispiele haben eines: eine zweite Maschine, die
`sim`-Outputs auf dieselben Adressen schreibt, von denen das Programm
liest. Der Testrahmen tickte eine Maschine — ohne das Modell bleiben die
Eingänge `Bad`, und verglichen würde ein Lauf, den es nicht gibt.

**Die Entscheidung war, dem Rahmen die Tickschleife der Runtime zu geben
statt eine Sonderbehandlung für Modelle.** Ein Plant-Modell ist nach 8.3
eine gewöhnliche Maschine; es braucht keinen eigenen Mechanismus, nur die
Kopplung über die Adresse, die die MIR ohnehin trägt. `harness::build_all`
führt jetzt alle Maschinen in Deklarationsreihenfolge, jede nach ihrer
Periode (7.2), und speist am Ende des Ticks `sim` nach `hw` — dieselbe
Naht, die der Interpreter in `apply_sim_bindings` zieht.

Die Alternative wäre gewesen, die Modellwerte als Stimulus vorzugeben.
Das hätte funktioniert und wäre falsch gewesen: Es prüft dann nicht mehr
dasselbe Programm, sondern eines, dem jemand die Eingaben vorgesagt hat.
Satz 9.4.4 spricht von *einer* Übersetzung, nicht von zwei Läufen mit
gemeinsamer Eingabe.

**14.1 und 14.4 laufen damit bitidentisch**, Plant-Modell inklusive. Die
übrigen vier scheitern an Konstrukten, die der Codegen noch nicht senkt
(FB-108) — und zwar sichtbar: Der Test in `beispiele.rs` führt sie mit
ihrem Grund und schlägt an, sobald eines läuft. Was fehlt, ist Teil des
Ergebnisses, nicht eine Fußnote.

### Drei Fehler auf dem Weg

**Eine Vorlage ohne Zustände stürzte ab** (FB-109). `dump_machine` und
`initial_leaf` indizierten `m.states[m.initial.index()]`; eine Vorlage hat
keine Zustände, ihr Rumpf entsteht erst in der Instanz (5.8). `takt mir`
stürzte damit auf jedem Programm mit `machine name(...)` ab — ein
Absturz statt einer Meldung, genau der Fall, den 4.1 für das Programm
ausschließt und den das Werkzeug sich selbst erlaubt hatte.

**Namen mit Sonderzeichen ergaben ungültige IR** (FB-110). FB-105 hatte
`fns::symbol` bereinigt — eine Stelle, nicht die Ursache. Dieselbe Lücke
steckte in den Blocktypen, Zustandstypen, Schrittfunktionen,
Zustandsmarken und nativen Symbolen: vierzehn Formatzeichenketten. Die
Bereinigung sitzt jetzt an den Namensfunktionen.

**Und der Rahmen verwarf weiterhin Abbruchgründe** — das hatte FB-104
schon behoben, und genau davon profitierte dieser Schritt: Statt
„undefined symbol" stand da `v1_step fehlt: after mit berechneter Dauer`.
Ohne diese Änderung hätte ich vier Linker-Fehler gehabt und keinen Grund.

### `takt graph`

Wer schreibt worauf, wer liest von wem, welche Kette läuft vor welcher?
Die Antworten stehen in der MIR verstreut — im `owner` eines Channels, in
den Cursorn einer Maschine, in `follows`. Das Kommando sammelt sie.

Die Ausgabe ist Text, keine Zeichnung: Sie soll in einen Bericht passen,
sich mit `diff` vergleichen lassen und ohne Werkzeug lesbar sein. Eine
Kante ist dabei besonders nützlich, weil sie im Quelltext am schwersten
zu sehen ist — die Simulationsbindung nach 8.3, deren beide
Deklarationen oft hundert Zeilen auseinanderstehen:

```
  Simulationsbindung (8.3)
      tank_p_sim -> tank_p
      chamber_p_sim -> chamber_p
```

**444 Tests.** Unter M4 bleibt FB-108: vier Konstrukte, vier Beispiele.


## 12.29 Vier Konstrukte, fünf Beispiele — und ein Befund über den DFA

FB-108 nannte vier Lücken für die vier übrigen Referenzbeispiele. Drei
sind erledigt; die vierte erwies sich als größer als die drei zusammen.

### Was klein war

**`for i in range(n)` im Maschinenrumpf** war gebaut, aber nur im
Funktionszweig verdrahtet. Die Schleife liegt jetzt auch im Maschinen-
Dispatcher, mit eigenem `break`-Stapel. Sie mit `fn_for` zu teilen hätte
geheißen, `Ctx` und `FnCtx` zu vereinen — die unterscheiden sich in
Fault-Pfad, Meldungsstellen und Blatt; bei zwölf Zeilen Rumpf wäre die
Naht teurer als die Wiederholung.

**`after` mit einem Maschinenparameter** war keine Lücke, sondern eine zu
enge Regel. Der Codegen verlangte ein Literal oder einen `param`, mit
Verweis auf die Schedulability — die begrenzt aber die *Kosten je
Aktivierung*, nicht die Fristen; eine `after`-Dauer geht in die
Budgetrechnung gar nicht ein. Grammatik (`duration_expr := expr`) und
Interpreter nehmen jeden `Duration`-Ausdruck, und eine engere Regel im
Codegen hieße, dass dasselbe Programm auf zwei Wegen verschieden
ausfällt.

**`repeat`/`step` in einer Sequenz** war überhaupt keine Lücke: Die MIR
senkt sie längst zu Zuständen mit Zähler. Der eigentliche Fehler lag im
Testrahmen (FB-111) — dazu unten.

Dabei fielen drei kleinere Ergänzungen an: `line<N>` als Typ (`str<N>`
plus `.truncated`, 3.9), `samples<T, N>` als beschränktes Array (8.9)
und `now` als Runtime-Aufruf. Letzteres gehört dorthin, weil alle
Maschinen dieselbe Uhr lesen — eine Kopie je Maschine wäre eine zweite
Quelle für dieselbe Zahl.

### Der Fehler, der eine Stunde kostete

**Der Testrahmen begann nicht im sicheren Zustand** (FB-111). Ein Lauf
startet mit den Outputs auf ihrem `safe`-Wert; der Interpreter tut das in
`Sim::new`, vor jedem Init. Der Rahmen nullte das Latch nur.

Beispiel 14.2 wechselte darum im Interpreter bei t=10 nach CYCLING und im
erzeugten Code nie: Die Bedingung `cell_v.valid and chamber_t.valid`
wurde nicht wahr, weil das Modell seine Werte aus einem leeren Latch
bezog. Die Suche ging durch Offsets, Perioden und Qualitätsbytes, die
alle stimmten — der Fehler lag davor. Dass er nie auffiel, liegt daran,
dass jeder bisherige `safe`-Wert zufällig 0 war.

### Was groß ist

**Der Mustervergleich** (FB-112) — und dabei kam ein Befund heraus, der
über 14.6 hinausgeht: `Pattern::Text` trägt `dfa: Option<Dfa>`, und das
Feld ist **im ganzen Programm nie `Some`**. Der Interpreter vergleicht
direkt über die `pieces`; der DFA aus 11.2 wird nirgends gebaut.

11.2 ist dabei eindeutig: „Handler-Dispatch als Schleife über das Fenster
mit vorkompilierten DFA-Tabellen", und die Tabellen sollen in `takt size`
erscheinen — dort stehen sie heute als `offen`. Der Vergleich gehört also
in den erzeugten Code, anders als die Stromzugriffe, die zurecht über
`takt_stream_*` laufen.

Das macht die Aufgabe zweischichtig: Erst muss M2 den DFA aus den
`pieces` bauen (der Typ steht seit M0 bereit), dann emittiert der Codegen
Klassenabbildung und Tabelle und extrahiert die Captures — im Interpreter
sind das 219 Zeilen Wertextraktion. `send` ist davon unabhängig und
kleiner: Serialisierung plus Überlaufprüfung (8.8).

**Fünf der sechs Beispiele laufen bitidentisch**, 444 Tests. Der M4-Exit
verlangt sechs; was fehlt, steht als FB-112 mit seinem Weg.


## 12.30 Der Produkt-DFA — gebaut, emittiert, und wozu er da ist

`Pattern::Text` trägt `dfa: Option<Dfa>` seit M0, und das Feld war im
ganzen Programm nie `Some`. Der Typ stand bereit, der Automat fehlte.

### Die Entwurfsfrage zuerst

**Wozu ein DFA, wenn der Interpreter schon linear läuft?** 8.7 sagt,
Matching und Extraktion seien „ein einziger Vorwärtsdurchlauf": Die
Mehrdeutigkeitsregel — auf `int`, `hex`, `float`, `word` folgt ein
Literal, dessen erstes Zeichen nicht zur Klasse gehört — macht jede
Capture-Grenze eindeutig. Der Interpreter braucht kein Backtracking, und
ein DFA *je Muster* brächte nichts.

Der Nutzen steht in 11.2 und 8.7 zusammen: „alle Muster der Handler eines
Zustands werden zu einem Produkt-DFA vereinigt, sodass jedes Element
einmal durchlaufen wird" — und das Budget eines Zustands mit Handlern ist
`CAP * (max_len + max_h cost(h.body))`, **`max_len` einmal**, nicht je
Handler. Ohne Produkt-DFA kostet ein Zustand mit drei Handlern drei
Durchläufe, und die Kostenrechnung aus 9.4.3 wäre zu optimistisch.

**Er ist also eine Kostenfrage, keine Korrektheitsfrage.** Das erklärt
auch, warum ohne ihn nie etwas falsch war.

### Gehört er in den Codegen?

Ja, und die Referenz sagt es dreifach: 11.2 verlangt „vorkompilierte
DFA-Tabellen", 11.5 ihre Ausweisung in `takt size`, 12.3 auf XIP-Zielen
eine RAM-Kopie. Alle drei setzen voraus, dass die Tabellen **Daten des
Programms** sind — eine Runtime hätte sie nicht zur Übersetzungszeit.

Die Stromzugriffe liegen dagegen zurecht in der Runtime (`takt_stream_*`):
Ihr gehören die Puffer. Die Trennung ist nicht Geschmack, sondern folgt
daraus, wem die Daten gehören.

### Was er liefert — und was nicht

**Ob** ein Muster trifft, nicht **was** in den Captures steht. Ein Automat
über Zeichenklassen kennt die Grenzen, aber nicht die Werte; `int` mit
seinen 19 Stellen und dem Überlauf als Nicht-Treffer wäre in einer
Tabelle nicht darstellbar. Die Extraktion bleibt der Vorwärtsdurchlauf —
eine zweite Maschinerie dafür wäre eine zweite Stelle, an der `int` seine
Grenzen bekommt (4.1).

Ein Muster mit offenem Ende (`str<N>`, `{_}`) bekommt gar keine Tabelle:
Es endet erst am Folgeliteral, und das kann der Durchlauf besser.
`build` sagt das durch `None`.

### Stand

Der Automat entsteht im Lowering, der Codegen emittiert Klassenabbildung
und Übergangstabelle als Konstanten und läuft sie je Element durch. Ein
Handler mit `matches` auf einem Literalmuster ist **nativ lauffähig** und
steht als `23_patterns` im differentiellen Korpus.

`takt size` weist die Tabellen aus: für Beispiel 14.6 sind es 5056 Byte,
`exakt` statt `offen`. Für `"READY"` entstehen sechs Alphabetklassen —
genau die Größenordnung, die 11.2 nennt („typisch 10–25 statt 256").

**Elf Tests für den Automaten**, davon fünf differentiell gegen
`match_text`: Wo Automat und Durchlauf dieselbe Frage beantworten, sagen
sie dasselbe. **455 Tests** insgesamt.

Offen bleibt die Extraktion (FB-113) — mit Grund: Der Codegen meldet
`Capture im Handler-Muster`, statt den Rumpf mit falschen Werten laufen
zu lassen. Ein falscher Wert ist schlimmer als eine Meldung.


## 12.31 Die Beispiele auf aarch64, und eine Quelle für den Mustervergleich

### Punkt 1: fünf EX-IDs belegt

Die Beispiele 14.1–14.5 standen auf `teilweise`, obwohl sie bitidentisch
laufen. Der Grund war nicht der Codegen, sondern die Abnahmeregel: 6.2
nennt eine EX-ID fertig, „wenn das Beispiel auf jeder bis dahin
unterstützten Zielklasse denselben Trace liefert" — und M4 trägt zwei.
Der aarch64-Test fuhr fünf Korpusdateien und **kein einziges Beispiel**.

`run_for` nimmt jetzt `Option<&str>`: eine Maschine wie bisher oder alle
— nötig für die fünf Beispiele mit Plant-Modell. Im Container gemessen:
alle zehn Programme laufen auf x86-64 und aarch64 bitidentisch. Die
EX-IDs stehen damit zu Recht auf `fertig`, und der M4-Exit ist für fünf
Sechstel belegt statt behauptet.

### Punkt 2: `takt-match`

Der Codegen braucht den Vorwärtsdurchlauf aus 8.7, den der Interpreter
schon hat. Ihn in LLVM IR nachzubauen wäre eine zweite Implementierung
derselben Regeln gewesen — zwei Gelegenheiten, `int` seine 19 Stellen
verschieden zu geben, wo Satz 9.4.4 Bit-Gleichheit verlangt.

**Prinzip 4 des Plans nennt das Vorbild:** `libtaktm` und `takt-native`
sind `no_std`-Crates ohne Abhängigkeiten, die Host-Interpreter und
Targets teilen. `takt-match` folgt dem. Es arbeitet auf Bytes statt
`&str`, weil ein `line<N>` im erzeugten Code ein Puffer ist und kein
Rust-String.

**Der differentielle Test fand beim ersten Lauf einen Fehler** (FB-114):
`-9223372036854775808` fiel auf `None` zurück, weil der Betrag positiv
aufgebaut und am Ende negiert wurde. Derselbe Fall wie FB-95 — der
Zweierkomplementbereich ist asymmetrisch. Dass er sofort auffiel, liegt
am Aufbau des Tests: Er vergleicht **Werte**, nicht nur Urteile.

`float` bleibt aus, mit Grund im Code: Eine eigene
Dezimal-nach-Binär-Konversion wäre eine zweite Rundungsquelle neben
`libtaktm`, und 4.2 verlangt bitgleiche Ergebnisse.

### Was die Verdrahtung aufhielt

Die Regeln stehen jetzt einmal, aber der Weg vom erzeugten Code dorthin
läuft über die Runtime — und **`unsafe_code = "forbid"` gilt
workspace-weit**. Ein `extern "C"` mit Rohzeigern in `takt-match` wäre
die erste Ausnahme und damit eine Entscheidung über die TCB (9.5), nicht
über ein Handler-Muster; 13.4 nennt das Verbot einen
Zertifizierungsgrund.

Dahinter lag ein zweiter Befund (FB-115): **Die Runtime hat noch keine
Ströme.** `takt-rt-core` ist die Tickschleife mit ihren Angelpunkten, im
Testrahmen ist `takt_stream_at` ein Stub. Das erklärt rückblickend,
warum der Handler-Dispatch im Codegen so weit gedieh, ohne dass etwas
auffiel: **Er wurde nie mit Daten ausgeführt.**

Die Reihenfolge steht damit fest: erst Ströme (der kurze Weg über den
Testrahmen, der lange über `takt-rt-core` — der trägt auch M5), dann
`takt_match` daneben, dann der Aufruf im Handler. Bis dahin meldet der
Codegen `Capture im Handler-Muster`, statt still zu rechnen.

**470 Tests**, davon 15 neu.


## 12.32 Der Strompuffer der Runtime (FB-115)

Der Handler-Dispatch im Codegen war gebaut und wurde **nie mit Daten
ausgeführt** — `takt_stream_at` ist im Testrahmen ein Stub. Das ist der
Befund, der diese Arbeit ausgelöst hat; er stand seit M4 unbemerkt da,
weil ein Stub grün ist wie alles andere.

### Die Entscheidung: geliehene Felder, keine Const-Generics

8.6 gibt jedem Strom seine eigenen Schranken (`capacity`,
`capacity_bytes`), und die stehen im Programm. Der Kern ist `no_std` und
allokiert nicht (12.1) — irgendwoher muss der Speicher also kommen.

**Const-Generics** wären der naheliegende Rust-Weg: `Ring<const CAP:
usize, const CAPB: usize>` trägt seine Größen im Typ, der Compiler kennt
sie, jede Grenze ist eine Konstante. Der Preis ist Monomorphisierung.
Ein Programm mit zwanzig Strömen verschiedener Größe bekäme zwanzig
Ausprägungen jeder Funktion, die einen Strom berührt — und das ist auf
dem heißen Pfad die Tickschleife selbst. 11.5 misst `takt size` in Byte;
12.3 rechnet mit XIP-Zielen, auf denen Flash knapp ist. Zwanzig Kopien
der Tickschleife sind dort kein Detail.

**Geliehene Felder** drehen es um: Der `Ring` hält zwei Slices, und wer
ihn baut, legt sie an — der Codegen in `.bss` (11.2), der Testrahmen als
Array, der Interpreter auf dem Heap. Ein Typ, eine Kopie des Codes, die
Größen als Laufzeitwerte. Das kostet je Zugriff eine Grenzprüfung, die
der Compiler bei Const-Generics wegrechnen könnte — aber bei
`unsafe_code = "forbid"` (13.4) steht diese Prüfung ohnehin, und sie ist
genau die Zusage aus 8.6: nie ein undefinierter Zustand.

### Zwei Formen, eine Struktur

8.6 unterscheidet feste Slots (`u8`, Records, `Edge`) und den Byte-Ring
(`line<N>`, `bytes<N>`). Getrennt gebaut wären das zwei Ringarithmetiken
mit denselben Randfällen. Sie teilen aber den Deskriptor `(seq, t,
offset, len)` und unterscheiden sich nur darin, ob `offset` in einen
Byte-Ring zeigt oder in ein Slot-Array. Der Code kennt deshalb **eine**
Struktur; wer feste Größen hat, setzt `len` konstant.

Ein Randfall verdient die Erwähnung: Ein Element, das allein die
Byteschranke sprengt, ist ein **Überlauf**, kein Verwerfen. Sonst
räumte `drop_oldest` den Ring leer und nähme das Element trotzdem nicht.

### Fünf Prüfwege

Der Ring rechnet mit Kopf, Länge und Modulo über geliehenen Feldern —
eine Stelle, an der sich Fehler nicht ansehen lassen. Darum mehr als
ein Weg:

1. **Die Regeln einzeln** (`tests/stream.rs`, 13 Tests): Der
   ASCII-Semantikblock aus 8.6/9.6 Zeile für Zeile — Reihenfolge und
   Nummerierung, das Fenster ab dem Cursor, `s.count` konsumiert nicht,
   beide Überlaufregeln, Eviction über das Minimum der Cursor, Inhalt
   über die Naht des Byte-Rings.
2. **Erschöpfend gegen ein Modell** (`tests/stream_erschoepfend.rs`):
   Bei drei Plätzen, vier Byte und vier Operationen je Schritt **jede**
   Folge bis zur Länge sechs — 4096 Abläufe, nach jedem Schritt ein
   Abgleich an vier Cursorpositionen gegen eine FIFO als schlichte
   Liste, ohne Umbruch und ohne Versatz. Wo beide auseinandergehen,
   liegt der Fehler in der Ringarithmetik.
3. **Invarianten unter Last** (`tests/stream_invarianten.rs`): 2500
   zufällige Schritte über fünf Saaten. Nach jedem gilt Lemma 9.6.1 —
   `|W| <= CAP`, Nummern streng steigend und lückenlos, hinter `end`
   ein leeres Fenster. Der Generator ist ein LCG im Test selbst: Der
   Kern hat keine Abhängigkeit, und die Tests achten dieselbe Grenze.
4. **Differentiell gegen den Interpreter**
   (`takt-interp/tests/ring_gleich.rs`): Zwei Implementierungen
   derselben FIFO — `VecDeque` auf dem Host, Ring im Kern. 400
   gemischte Operationen mit Vergleich nach jedem Schritt. Gingen sie
   auseinander, wäre Satz 9.4.4 verletzt.
5. **Bare Metal**: `thumbv7em-none-eabihf` installiert;
   `takt-rt-core`, `takt-match` und `takt-native` bauen sauber für
   Cortex-M4F.

### Was das Target nebenbei löst

`libtaktm` baut für Cortex-M4F nur mit `--no-default-features` — `sqrt`,
`fma` und die Rundungen sind LLVM-Intrinsics aus `std`. Der
Feature-Block sagt seit M4, die Software-Emulation komme mit M5, „wenn
ein `no_std`-Target existiert, an dem sie sich prüfen lässt". Das Target
existiert jetzt; die M5-Aufgabe ist damit prüfbar geworden.

### Was offen bleibt

Der Puffer steht, verdrahtet ist er nicht. Der Testrahmen bildet
`takt_stream_count`, `_at` und `_examined` noch auf Stubs ab — das ist
der nächste Schritt, und erst danach läuft ein Handler nativ mit Daten.

**492 Tests**, davon 23 für den Strompuffer.


## 12.33 Die Ströme am erzeugten Code (FB-115, FB-116)

Der Puffer stand (12.32), verdrahtet war er nicht: `takt_stream_count`
lieferte null. Diesen Abschnitt kostet die Verdrahtung — und sie hat
einen Fehler im Codegen freigelegt, der zwei Meilensteine lang unsichtbar
war.

### Die Entscheidung: eine Tabelle, kein Ring

Der Rahmen erzeugt C, der Ring ist Rust. Drei Wege standen offen:

1. **Den Ring in C nachbauen** — eine zweite Implementierung der
   8.6-Regeln, also genau die Gefahr, gegen die `takt-match` gebaut
   wurde. Verworfen.
2. **`takt-rt-core` über `extern "C"` rufen** — der `void *out` der ABI
   braucht einen Rohzeiger und damit `unsafe`. Das wäre die erste
   Ausnahme in einem Workspace, der es verbietet, und 13.4 nennt das
   Verbot einen Zertifizierungsgrund. Verworfen, aus demselben Grund wie
   bei FB-113.
3. **Eine Tabelle über dem Stimulus** — gewählt.

Der dritte Weg nutzt aus, was den Testrahmen vom echten Ziel
unterscheidet: **Der Stimulus steht vor dem Lauf fest.** Ein Treiber
liefert, wann er will; der Rahmen weiß es vorher. Damit braucht die
C-Seite keinen Ring, sondern eine Tabelle und je Konsument einen Cursor —
die Verdrängung entfällt, weil nichts nachrückt.

Der Ring bleibt dabei die eine Implementierung für echte Ziele. Was der
Rahmen nachrechnet, sind nur die *beobachtbaren* Folgen der Schranken:
`capacity` und `capacity_bytes` sind in 8.6 nicht bloß Speicher, sondern
erzeugen `s.overflowed` und einen Fault. Er rechnet sie je Tick nach,
unter der Annahme eines Konsumenten, der sein Fenster leert — der Fall,
den der erzeugte Code herstellt. Ein Puffer, der über mehrere Ticks
vollläuft, weil niemand liest, bräuchte den Cursor des Konsumenten, und
den kennt erst der Lauf. Das steht als Grenze in `limits.rs`, wo vorher
„`takt_stream_count` liefert 0" stand.

### Der Stimulus bekommt einen Typ

`(Tick, Name)` reichte für ein Command, das nur aus seinem Namen besteht
(8.5). Ein Stromelement trägt den Kanal *und* den Wert. Ein drittes Feld
im Tupel hätte jeden Aufrufer belastet, und `("go", "")` wäre ein Command
mit leerem Wert oder ein Element ohne — am Typ nicht zu unterscheiden.
`Stimulus` ist die Unterscheidung, die der Trace längst kennt (`cmd`
gegen `in`, 9.3).

### FB-116: Der Cursor wurde nie zurückgeschrieben

Beim Verdrahten fiel auf, was vorher nicht auffallen konnte. Der
Handler-Dispatch lädt `cur[s, m]`, läuft über das Fenster und meldet
jedes Element mit `takt_stream_examined` — **aber er speichert den
fortgeschrittenen Cursor nicht.** Eine Maschine sähe dieselben Elemente
in jedem Tick wieder.

Der Kommentar an der Stelle sagte, die Runtime führe den Cursor mit
`examined` nach. Das stimmt zur Hälfte: Die Runtime braucht `examined`
für die Eviction, weil nur sie das Minimum über *alle* Konsumenten kennt.
Der Cursor selbst steht im Zustands-Struct der Maschine, und den schreibt
allein der erzeugte Code. 9.6 führt beides als zwei Schritte —
`advance_cursors()` und die Eviction danach.

Unsichtbar war der Fehler, weil ein Cursor, der sich nicht bewegt, bei
leerem Fenster nicht auffällt. Er lag seit M4 im Code.

### Wie der Test dazu gebracht wurde, ihn zu sehen

Der erste Versuch war grün — auch ohne das Rückschreiben. Der Grund:
`23_patterns` setzte `y = 7`, und ein Element mehrfach zu sehen ergibt
denselben Wert. Ein Test, der einen echten Fehler nicht bemerkt, ist
kein Test.

Die Datei zählt jetzt die untersuchten Elemente in einen Output. Damit
ist der Unterschied im Trace sichtbar, und die Gegenprobe belegt es:
**ohne das Rückschreiben zeigt der Vergleich 57 Abweichungen.**

### Zwei Prüfwege

1. **Differentiell mit Daten** (`differential.rs`): `23_patterns` läuft
   auf beiden Seiten mit zwei Elementen — eines trifft das Muster, eines
   nicht. Der Test sichert zusätzlich zu, dass der Handler überhaupt
   lief; sonst wäre ein Lauf, in dem beide Seiten nichts tun, ebenfalls
   grün.
2. **Der erzeugte C-Text** (`tests/stroeme.rs`, 6 Tests): Nummerierung,
   Kürzung am Rand (3.9), beide Schranken aus 8.6, und dass ein Stimulus
   für einen anderen Kanal nichts anfasst. Diese laufen auch ohne clang.

Dabei zeigte sich SC-17 von seiner nützlichen Seite: `capacity_bytes = 4`
bei `line<16>` wird statisch abgelehnt, weil Lemma 9.6.1 mehr verlangt.
Der Test musste seine Zahlen korrigieren, nicht die Prüfung.

**498 Tests**, davon 7 neu.


## 12.34 Mustervergleich, Extraktion und `send` (FB-112, FB-113)

Der Automat stand seit 12.31, die Ströme seit 12.33. Was fehlte, war die
Extraktion — und die Naht, an der sie hängen sollte.

### Die Naht, die nicht zu ziehen war

`takt-match` hält die Regeln aus 8.7, und der naheliegende Weg wäre, sie
aus dem erzeugten Code zu *rufen*. Er scheitert an der Form des Aufrufs:
Die Bausteine müssten als Feld von Deskriptoren über die C-ABI gehen,
also über Rohzeiger — und `unsafe_code = "forbid"` gilt workspace-weit,
was 13.4 einen Zertifizierungsgrund nennt. Ein `extern "C"` in
`takt-match` wäre eine Entscheidung über die TCB (9.5), nicht über ein
Handler-Muster.

**11.2 zeigt, dass hier ohnehin die richtige Seite ist.** Der
Handler-Dispatch läuft „mit vorkompilierten DFA-Tabellen", die in
`takt size` erscheinen (11.5) und auf XIP-Zielen im RAM liegen (12.3).
Der Vergleich gehört damit in den erzeugten Code. Die Extraktion gehört
zu demselben Muster; sie über eine Naht zu schieben hieße, ein Muster auf
zwei Seiten zu bearbeiten.

### `takt-match` als Orakel statt als geteilter Code

Damit gibt es zwei Implementierungen derselben Regeln — genau das, wovor
`takt-match` schützen sollte. Der Unterschied liegt in der Prüfung:
`crates/takt-llvm/tests/captures.rs` übersetzt je Muster eine kleine
Funktion, lässt sie über viele Texte laufen und vergleicht **Urteil und
Werte** gegen `takt-match`. Neun Muster gegen 28 Texte sind 252
Vergleiche je Lauf.

Das ist die schärfere Prüfung, nicht die schwächere: Zwei
Implementierungen, die übereinstimmen müssen, decken auf, was eine
gemeinsame stillschweigend teilt. FB-114 wurde so gefunden.

**Der Test fand beim ersten Lauf fünf Fehler:**

1. Das Vorzeichen wurde hinter dem falschen Index gesucht — `-1` kam als
   `2531` heraus, weil `parse_int` die Spanne *vor* dem Minus bekam und
   dann noch einmal zurückschaute.
2. `0x` wurde nicht übersprungen: `0x7E8` las das `x` als Hexziffer.
3. `0X` wurde akzeptiert, wo `takt-match` ablehnt.
4. Ein leeres `word` galt als Treffer.
5. Der Treiber las ein `i1` in ein C-`int` — die oberen 31 Bit sind
   undefiniert, und ein falsches Urteil las sich als wahr. Der einzige
   der fünf, der im Test selbst saß.

Vier der fünf hätte kein Urteilsvergleich gefunden: Das Muster traf
richtig, nur der *Wert* war falsch.

### `send` und die Formatstrings

`send o, "UPDATE {size} {crc:hex}\n"` schreibt in einen Puffer fester
Höchstlänge (8.8). `{x}`, `{x:hex}` und `{x:04}` entstehen als IR;
`float` bleibt aus, mit demselben Grund wie überall: Die kürzeste
wiedereinlesbare Dezimaldarstellung ist ein eigener Algorithmus (Grisu,
Ryū), und eine zweite Implementierung wäre eine zweite Rundungsquelle
neben `libtaktm` — 4.2 verlangt bitgleiche Ergebnisse.

Ein Rand verdient die Erwähnung: `{x:hex}` von `-2` ist
`fffffffffffffffe`, nicht `-2`. Hex zählt das Bitmuster, und der
Interpreter schreibt `{:x}` von `*i as u64`. Die erste Fassung rechnete
mit `sdiv` und einer Abbruchbedingung „noch negativ" — sie gab `0`
zurück. Der Korpusvergleich fand es.

### Der Sendepuffer im Testrahmen

Zuerst schrieb der Rahmen jedes `send` sofort in den Trace. Der
Interpreter tut etwas anderes: 8.8 sagt, der Treiber leert den Puffer mit
`max_rate`, und was er abholt, erscheint als `out <stream> [bytes]`. Bei
1000 Hz und 10 ms Tick sind das zehn Byte je Tick — der Text kam also
häppchenweise, während der Rahmen ihn auf einmal ausgab.

Der Rahmen rechnet jetzt dieselbe Rate. Damit ist `send` kein
Sonderfall mehr: Es erscheint als `out`-Zeile, und `compare` prüft es
gegen den Interpreter wie jeden anderen Ausgang.

### Was 14.6 noch fehlt

Zwei Konstrukte, beide außerhalb des Mustervergleichs: `i_dut.max()` auf
`samples<float[A], 4>` (8.9) und `every d:` im Modell (5.8). Sie sind
eigene Aufgaben; `beispiele.rs` meldet von selbst, sobald 14.6 läuft.

**501 Tests**, davon 2 neu; dazu zwei Korpusprogramme (`24_send_has`,
`25_format`).

### Ein Nebenbefund am Register

`plan/feedback.csv` hatte in FB-115 eine Zeile mit 14 statt 12 Feldern:
Ein Anführungszeichen war beim Schreiben als `\"` in die Datei geraten,
und der Parser zerlegte das Feld an zwei Kommas. Repariert; ein
Feldzähler über alle Zeilen findet so etwas sofort und gehört in jede
Registeränderung.


## 12.35 Reduktionen, `every` und ein Symbol, das nie deklariert war

EX-14.6 ist das letzte offene Beispiel in M4; `features.csv` führt vier
seiner sechs `teilweise`-Einträge auf es zurück. Gemessen fehlten ihm
genau zwei Konstrukte — `i_dut.max()` auf `samples<float[A], 4>` (8.9)
und `every 20 ms:` im Plant-Modell (5.8). Beide sind im Interpreter
definiert, es gab also für beide ein Orakel.

### Reduktionen: die Summationsreihenfolge ist Semantik

`min`, `max`, `mean`, `rms`, `count` und `last` stehen jetzt in
`takt-llvm::reduce`. Die Länge steht im Typ, also ist jede Reduktion
statisch beschränkt (4.1), und der Code rollt sie ab: Die Kette aus
Vergleich und `select` hat keine Verzweigung, LLVM vektorisiert sie, und
eine Schleife bräuchte drei Marken und einen Zähler für dasselbe
Ergebnis.

**Die eine Stelle, an der es nicht um Geschmack geht**, ist die
Summation von `mean` und `rms`. Sie läuft von links nach rechts, weil
`Ctx::reduce` im Interpreter es so tut. Eine paarweise Summation wäre
numerisch besser — und liefe gegen 4.2, das bitgleiche Ergebnisse über
alle Targets verlangt. Dass das trägt, zeigt der Vergleich: `rms` über
`[3, -1, 4, 2]` ergibt auf beiden Seiten `2.7386127875258306`, bis zur
letzten Stelle.

Für `min`/`max` kein `llvm.minnum`: Es behandelt NaN eigen, und 4.1
schließt NaN zwar aus, aber die Zusage steht in der Analyse, nicht im
erzeugten Code. Ein geordneter Vergleich bleibt auch ohne sie richtig.

Für die Wurzel dagegen **schon** ein Intrinsic. IEEE-754 verlangt für
`sqrt` ein korrekt gerundetes Ergebnis, und ein korrekt gerundetes
Ergebnis ist eindeutig — genau die Begründung, mit der die Referenz
`libtaktm` rechtfertigt (Abschnitt 16). `libtaktm::sqrt_f64` ruft
seinerseits nur `x.sqrt()`; der Umweg über die Runtime brächte dieselbe
Zahl und ein Symbol, das jedes Profil bereitstellen müsste.

### `every`: der Zustand war schon dafür gebaut

`Role::EveryNext` gab es seit M4 im Zustands-Struct — ein `i64` je
Aufrufstelle, wie 11.2 es vorsieht. Es fehlte nur das Senken der
Anweisung.

Die Uhr hängt am Ort: `t_in_state` für eine Stelle in einem Zustand,
`now` im maschinenweiten `loop:` (5.8). Der Grund steht dort und ist
beobachtbar — ein Block im Maschinen-`loop:` gehört keinem Zustand,
dessen Eintritt ihn neu startete, und verstummte mit `t_in_state` nach
dem ersten Wechsel. `27_every` prüft beide Formen nebeneinander.

**Der Zähler beginnt bei `-1`, nicht bei null.** Der Interpreter löscht
seinen Eintrag beim Zustandseintritt, sodass `at_or` wieder mit `d`
anfängt. Im erzeugten Code steht er als Feld, und null wäre ein
gültiger Zeitpunkt: Der Block liefe schon im Eintritts-Tick statt nach
`d`. `-1` heißt „noch nicht gesetzt", und der erste Durchlauf trägt `d`
ein.

Das Vorrücken geht **vom fälligen Zeitpunkt** aus, nicht von der Uhr —
sonst driftete das Raster, wenn eine Aktivierung ausfiel.

### Was der Weg zu 14.6 sonst noch freilegte

Zwei Funde, beide unbemerkt:

**`verdict` war nicht gesenkt.** Es steht in 13.2 neben `measure` und
`verify`, und Leitentscheidung 14 fasst die drei zusammen: „Beobachtung
ist von Steuerung getrennt … können nie einen Fault auslösen." Die
anderen beiden waren da. Mit `verdict` ist `Observe` vollständig, und
der Fallback-Zweig `_ => Err(…)` konnte entfallen — der Compiler hält
die Vollständigkeit jetzt selbst fest.

**FB-117: `takt_now` wurde gerufen, aber nie deklariert.** Der Codegen
senkt `now` seit M4 als Aufruf, und `Abi::declare` führte das Symbol
nicht. Jede IR mit `now` war damit ungültig. Unsichtbar war es, weil
**kein Korpusprogramm `now` las** — der Aufruf entstand nie, also fehlte
auch die Deklaration nie. `27_every` benutzt `now` und fängt den Fall
ab, sobald er wiederkommt.

### Was 14.6 noch fehlt

Ein Konstrukt: `at T:` (9.8, geplante Ausgabe). Es braucht die
`sched`-Warteschlangen aus 11.2, die der Codegen noch nicht hat — ein
eigenes Stück Arbeit, größer als die beiden hier. `dut_model` senkt
inzwischen vollständig; `brownout_test` bleibt daran hängen.

**503 Tests**; Korpus um `26_samples` und `27_every` gewachsen.


## 12.36 EX-14.6 läuft — die letzten fünf Konstrukte

Nach 12.35 fehlte 14.6 noch `at T:`. Beim Senken kamen vier weitere zum
Vorschein, jedes erst sichtbar, nachdem das davor gelöst war. Das ist
keine schlechte Planung, sondern die Eigenart eines Abbruchs beim ersten
Fehler: `step_function` meldet, woran sie hängenbleibt, nicht alles, was
noch käme.

### `at T:` — die Warteschlange gehört der Runtime

Die Frage war, wo `sched` liegt. Der Interpreter hält es im
Prozessabbild, der naheliegende Codegen-Weg wäre der Maschinenzustand.
**11.2 entscheidet es wörtlich:** „`sched`-Warteschlangen als feste
Arrays im Runtime-Anteil des Outputs."

Das folgt der Zuständigkeit. `apply_scheduled(k)` läuft zu Tick-Beginn
über *alle* Outputs, bevor irgendeine Maschine schreitet (9.8) — ein
Tickschritt kann das nicht tun. Und die beiden Faults, die dabei
entstehen können (`TimingFault`, `ScheduleOverflow`), entscheidet
ebenfalls nur die Runtime: Sie kennt die Warteschlange, der erzeugte
Code nicht. Er prüft darum das Ergebnis von `takt_schedule` und nimmt
bei `false` seinen Fault-Pfad; eine eigene Prüfung wäre eine zweite
Meinung über eine Datenstruktur, die er nicht sieht.

`takt size` rechnete schon vorher mit K_o = 4 Einträgen à 16 Byte je
Output — die Implementierung folgt dieser Rechnung, sonst löge der
Bericht.

### `->` als Anweisung — eine Stelle für beide Auslöser

Der Zustandswechsel stand vollständig in `transitions`: `exit:`-Kette,
Aktionsblock, `enter:`-Kette, `conf` setzen, Zähler zurück. Ein `->` im
Block tut dasselbe.

Statt es zu wiederholen, ist der Rumpf jetzt `enter_leaf`, und beide
Auslöser rufen ihn. Zwei Kopien wären zwei Gelegenheiten, 5.2
verschieden auszulegen — und 5.2 hat genug Reihenfolge, dass das
passieren würde.

**Der Entry-Modus war die Feinheit.** Der Interpreter liefert
`Out::Goto` nur im `Run`-Modus; im Entry-Tick ist ein `->` wirkungslos
(5.2 Regel 4). Der Codegen senkt die `loop:`-Blöcke des Entry-Ticks aus
demselben MIR-Block, also musste er dieselbe Unterscheidung treffen:
`Ctx::end` trägt das Sprungziel und wird für die Dauer dieser Blöcke
entfernt — an drei Stellen, denn der Fault-Pfad hat seinen eigenen
Entry-Tick.

### Muster-Guard — ein Guard fragt, er verarbeitet nicht

`until … matches` sucht das **erste** passende Element und hält dort an.
Der Unterschied zum Handler-Dispatch ist nicht kosmetisch: Der Dispatch
untersucht jedes Element des Fensters, ein Guard nur, bis er eine
Antwort hat. Was danach kommt, bleibt unkonsumiert (8.7).

Der Cursor rückt darum nur bis zum Treffer — und mit dem Maximum, weil
`dispatch` für denselben Strom in diesem Tick schon gelaufen sein kann:
9.7 sagt, `examined` ist das Maximum über alle Konstrukte der
Aktivierung.

### Textvergleich — was hinter der Länge steht, zählt nicht

`r.outcome == "OK"` braucht zwei Dinge: ein Textliteral als Wert und die
Gleichheit zweier `str<N>`. Beide sind `{ i32 len, [N x i8] }`, und ein
`icmp eq` auf dem ganzen Struct verglich auch die Bytes hinter der
Länge — bei einem Capture sind das Reste des vorigen Elements. Der
Vergleich läuft darum über Länge und Inhalt bis dorthin, wie
`Value::Str` im Interpreter.

### Der Stand

**Alle sechs Beispiele, die M4 trägt, laufen** und stimmen mit dem
Interpreter überein. `OPEN` in `examples.rs` ist leer; der Test, der
meldet, wenn ein offenes Beispiel unerwartet läuft, hat 14.6 von selbst
angezeigt.

Was für den vollständigen M4-Exit noch aussteht: der aarch64-Lauf für
14.6. Er braucht die Werkzeugkette im Container (`tools/linux.sh`), und
Docker lief bei dieser Sitzung nicht — `targets.rs` führt 14.6 bereits
in seiner Liste und überspringt sich ohne `qemu-aarch64`. Der Test ist
geschrieben, nur nicht ausgeführt.

**503 Tests**; Korpus um `28_scheduled` gewachsen.
